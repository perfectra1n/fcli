// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `repo` operations: `releases`.
use forgejo_core::Result;
use forgejo_core::http::{Body, ItemStream, PageInfo, Paging, Part, Progress, Request, encode};
impl super::Repo<'_> {
    /// Create a release
    ///
    /// `POST /repos/{owner}/{repo}/releases` — needs the `write:repository` token scope.
    pub async fn create_release(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::CreateReleaseOption,
    ) -> Result<forgejo_model::Release> {
        let req =
            Request::post(format!("/repos/{}/{}/releases", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a release attachment
    ///
    /// `POST /repos/{owner}/{repo}/releases/{id}/assets` — needs the `write:repository` token scope.
    #[allow(
        clippy::too_many_arguments,
        reason = "every argument is one this API operation requires; a struct would hide its shape"
    )]
    pub async fn create_release_attachment(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        attachment: Option<Part>,
        external_url: Option<&str>,
        query: &crate::query::RepoCreateReleaseAttachmentQuery,
        progress: Progress,
    ) -> Result<forgejo_model::Attachment> {
        let mut parts: Vec<Part> = Vec::from([]);
        if let Some(attachment) = attachment {
            parts.push(Part { name: "attachment".to_owned(), ..attachment });
        }
        if let Some(external_url) = external_url {
            parts.push(Part::bytes("external_url", external_url.as_bytes().to_vec()));
        }
        let req = query.apply(
            Request::post(format!(
                "/repos/{}/{}/releases/{}/assets",
                encode::seg(owner),
                encode::seg(repo),
                id
            ))
            .scope("write:repository")
            .body(Body::Multipart(parts))
            .progress(progress),
        );
        self.client.json(req).await
    }
    /// Create a new git tag in a repository
    ///
    /// `POST /repos/{owner}/{repo}/tags` — needs the `write:repository` token scope.
    pub async fn create_tag(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::CreateTagOption,
    ) -> Result<forgejo_model::Tag> {
        let req =
            Request::post(format!("/repos/{}/{}/tags", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Delete a release
    ///
    /// `DELETE /repos/{owner}/{repo}/releases/{id}` — needs the `write:repository` token scope.
    pub async fn delete_release(&self, owner: &str, repo: &str, id: i64) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/releases/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a release attachment
    ///
    /// `DELETE /repos/{owner}/{repo}/releases/{id}/assets/{attachment_id}` — needs the `write:repository` token scope.
    pub async fn delete_release_attachment(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        attachment_id: i64,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/releases/{}/assets/{}",
            encode::seg(owner),
            encode::seg(repo),
            id,
            attachment_id
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a release by tag name
    ///
    /// `DELETE /repos/{owner}/{repo}/releases/tags/{tag}` — needs the `write:repository` token scope.
    pub async fn delete_release_by_tag(&self, owner: &str, repo: &str, tag: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/releases/tags/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(tag)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a repository's tag by name
    ///
    /// `DELETE /repos/{owner}/{repo}/tags/{tag}` — needs the `write:repository` token scope.
    pub async fn delete_tag(&self, owner: &str, repo: &str, tag: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/tags/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(tag)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Update a release
    ///
    /// `PATCH /repos/{owner}/{repo}/releases/{id}` — needs the `write:repository` token scope.
    pub async fn edit_release(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        body: &forgejo_model::EditReleaseOption,
    ) -> Result<forgejo_model::Release> {
        let req = Request::patch(format!(
            "/repos/{}/{}/releases/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Edit a release attachment
    ///
    /// `PATCH /repos/{owner}/{repo}/releases/{id}/assets/{attachment_id}` — needs the `write:repository` token scope.
    pub async fn edit_release_attachment(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        attachment_id: i64,
        body: &forgejo_model::EditAttachmentOptions,
    ) -> Result<forgejo_model::Attachment> {
        let req = Request::patch(format!(
            "/repos/{}/{}/releases/{}/assets/{}",
            encode::seg(owner),
            encode::seg(repo),
            id,
            attachment_id
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Gets the most recent non-prerelease, non-draft release of a repository, sorted by created_at
    ///
    /// `GET /repos/{owner}/{repo}/releases/latest` — needs the `read:repository` token scope.
    pub async fn get_latest_release(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<forgejo_model::Release> {
        let req = Request::get(format!(
            "/repos/{}/{}/releases/latest",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get a release
    ///
    /// `GET /repos/{owner}/{repo}/releases/{id}` — needs the `read:repository` token scope.
    pub async fn get_release(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
    ) -> Result<forgejo_model::Release> {
        let req = Request::get(format!(
            "/repos/{}/{}/releases/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get a release attachment
    ///
    /// `GET /repos/{owner}/{repo}/releases/{id}/assets/{attachment_id}` — needs the `read:repository` token scope.
    pub async fn get_release_attachment(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        attachment_id: i64,
    ) -> Result<forgejo_model::Attachment> {
        let req = Request::get(format!(
            "/repos/{}/{}/releases/{}/assets/{}",
            encode::seg(owner),
            encode::seg(repo),
            id,
            attachment_id
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get a release by tag name
    ///
    /// `GET /repos/{owner}/{repo}/releases/tags/{tag}` — needs the `read:repository` token scope.
    pub async fn get_release_by_tag(
        &self,
        owner: &str,
        repo: &str,
        tag: &str,
    ) -> Result<forgejo_model::Release> {
        let req = Request::get(format!(
            "/repos/{}/{}/releases/tags/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(tag)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get the tag of a repository by tag name
    ///
    /// `GET /repos/{owner}/{repo}/tags/{tag}` — needs the `read:repository` token scope.
    pub async fn get_tag(&self, owner: &str, repo: &str, tag: &str) -> Result<forgejo_model::Tag> {
        let req = Request::get(format!(
            "/repos/{}/{}/tags/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(tag)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// List release's attachments
    ///
    /// `GET /repos/{owner}/{repo}/releases/{id}/assets` — needs the `read:repository` token scope.
    pub async fn list_release_attachments(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
    ) -> Result<Vec<forgejo_model::Attachment>> {
        let req = Request::get(format!(
            "/repos/{}/{}/releases/{}/assets",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("read:repository");
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::Attachment>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// List a repo's releases
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/releases` — needs the `read:repository` token scope.
    pub fn list_releases(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListReleasesQuery,
    ) -> ItemStream<forgejo_model::Release> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/releases",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a repo's releases
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/releases` — needs the `read:repository` token scope.
    pub async fn list_releases_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListReleasesQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Release>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/releases",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List a repository's tags
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/tags` — needs the `read:repository` token scope.
    pub fn list_tags(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListTagsQuery,
    ) -> ItemStream<forgejo_model::Tag> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/tags", encode::seg(owner), encode::seg(repo)))
                    .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a repository's tags
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/tags` — needs the `read:repository` token scope.
    pub async fn list_tags_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListTagsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Tag>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/tags", encode::seg(owner), encode::seg(repo)))
                    .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
}
