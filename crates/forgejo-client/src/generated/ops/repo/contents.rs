// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `repo` operations: `contents`.
use forgejo_core::Result;
use forgejo_core::http::{Accept, ByteStream, Mime, Request, encode};
impl super::Repo<'_> {
    /// Modify multiple files in a repository
    ///
    /// `POST /repos/{owner}/{repo}/contents` — needs the `write:repository` token scope.
    pub async fn change_files(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::ChangeFilesOptions,
    ) -> Result<forgejo_model::FilesResponse> {
        let req =
            Request::post(format!("/repos/{}/{}/contents", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a file in a repository
    ///
    /// `POST /repos/{owner}/{repo}/contents/{filepath}` — needs the `write:repository` token scope.
    pub async fn create_file(
        &self,
        owner: &str,
        repo: &str,
        filepath: &str,
        body: &forgejo_model::CreateFileOptions,
    ) -> Result<forgejo_model::FileResponse> {
        let req = Request::post(format!(
            "/repos/{}/{}/contents/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::path_like(filepath)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Delete a file in a repository
    ///
    /// `DELETE /repos/{owner}/{repo}/contents/{filepath}` — needs the `write:repository` token scope.
    pub async fn delete_file(
        &self,
        owner: &str,
        repo: &str,
        filepath: &str,
        body: &forgejo_model::DeleteFileOptions,
    ) -> Result<forgejo_model::FileDeleteResponse> {
        let req = Request::delete(format!(
            "/repos/{}/{}/contents/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::path_like(filepath)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Gets the metadata and contents (if a file) of an entry in a repository, or a list of entries if
    /// a dir
    ///
    /// `GET /repos/{owner}/{repo}/contents/{filepath}` — needs the `read:repository` token scope.
    pub async fn get_contents(
        &self,
        owner: &str,
        repo: &str,
        filepath: &str,
        query: &crate::query::RepoGetContentsQuery,
    ) -> Result<forgejo_model::ContentsResponseOrList> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/contents/{}",
                encode::seg(owner),
                encode::seg(repo),
                encode::path_like(filepath)
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Gets the metadata of all the entries of the root dir
    ///
    /// `GET /repos/{owner}/{repo}/contents` — needs the `read:repository` token scope.
    pub async fn get_contents_list(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoGetContentsListQuery,
    ) -> Result<Vec<forgejo_model::ContentsResponse>> {
        let req = query.apply(
            Request::get(format!("/repos/{}/{}/contents", encode::seg(owner), encode::seg(repo)))
                .scope("read:repository"),
        );
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::ContentsResponse>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Get a file from a repository
    ///
    /// `GET /repos/{owner}/{repo}/raw/{filepath}` — needs the `read:repository` token scope.
    pub async fn get_raw_file(
        &self,
        owner: &str,
        repo: &str,
        filepath: &str,
        query: &crate::query::RepoGetRawFileQuery,
    ) -> Result<(Mime, ByteStream)> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/raw/{}",
                encode::seg(owner),
                encode::seg(repo),
                encode::path_like(filepath)
            ))
            .scope("read:repository")
            .accept(Accept::Any),
        );
        self.client.bytes(req).await
    }
    /// Get a file or it's LFS object from a repository
    ///
    /// `GET /repos/{owner}/{repo}/media/{filepath}` — needs the `read:repository` token scope.
    pub async fn get_raw_file_or_lfs(
        &self,
        owner: &str,
        repo: &str,
        filepath: &str,
        query: &crate::query::RepoGetRawFileOrLfsQuery,
    ) -> Result<(Mime, ByteStream)> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/media/{}",
                encode::seg(owner),
                encode::seg(repo),
                encode::path_like(filepath)
            ))
            .scope("read:repository")
            .accept(Accept::Any),
        );
        self.client.bytes(req).await
    }
    /// Update a file in a repository
    ///
    /// `PUT /repos/{owner}/{repo}/contents/{filepath}` — needs the `write:repository` token scope.
    pub async fn update_file(
        &self,
        owner: &str,
        repo: &str,
        filepath: &str,
        body: &forgejo_model::UpdateFileOptions,
    ) -> Result<forgejo_model::FileResponse> {
        let req = Request::put(format!(
            "/repos/{}/{}/contents/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::path_like(filepath)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
}
