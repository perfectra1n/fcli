// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `repo` operations: `actions`.
use forgejo_core::Result;
use forgejo_core::http::{Accept, ByteStream, ItemStream, Mime, PageInfo, Paging, Request, encode};
impl super::Repo<'_> {
    /// Create a repo-level variable
    ///
    /// `POST /repos/{owner}/{repo}/actions/variables/{variablename}` — needs the `write:repository` token scope.
    pub async fn create_repo_variable(
        &self,
        owner: &str,
        repo: &str,
        variablename: &str,
        body: &forgejo_model::CreateVariableOption,
    ) -> Result<()> {
        let req = Request::post(format!(
            "/repos/{}/{}/actions/variables/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(variablename)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.empty(req).await
    }
    /// Delete a particular runner that belongs to a repository
    ///
    /// `DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}` — needs the `write:repository` token scope.
    pub async fn delete_repo_runner(&self, owner: &str, repo: &str, runner_id: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/actions/runners/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(runner_id)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a secret in a repository
    ///
    /// `DELETE /repos/{owner}/{repo}/actions/secrets/{secretname}` — needs the `write:repository` token scope.
    pub async fn delete_repo_secret(
        &self,
        owner: &str,
        repo: &str,
        secretname: &str,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/actions/secrets/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(secretname)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a repo-level variable
    ///
    /// `DELETE /repos/{owner}/{repo}/actions/variables/{variablename}` — needs the `write:repository` token scope.
    pub async fn delete_repo_variable(
        &self,
        owner: &str,
        repo: &str,
        variablename: &str,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/actions/variables/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(variablename)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Download the plaintext logs of an action job Returns the plaintext log for the job. By default
    /// the log for the most recent attempt is returned (ActionRunJob.TaskID tracks the latest task).
    /// Pass `?attempt=N` to fetch the log for a specific historical attempt; the value matches the
    /// `attempt` field returned by the job listing endpoints.
    ///
    /// `GET /repos/{owner}/{repo}/actions/jobs/{job_id}/logs` — needs the `read:repository` token scope.
    pub async fn get_action_job_logs(
        &self,
        owner: &str,
        repo: &str,
        job_id: i64,
        query: &crate::query::RepoGetActionJobLogsQuery,
    ) -> Result<String> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/actions/jobs/{}/logs",
                encode::seg(owner),
                encode::seg(repo),
                job_id
            ))
            .scope("read:repository")
            .accept(Accept::Text),
        );
        crate::text(self.client, req).await
    }
    /// Download a ZIP of plaintext logs for every job in an action run
    ///
    /// `GET /repos/{owner}/{repo}/actions/runs/{run_id}/logs` — needs the `read:repository` token scope.
    pub async fn get_action_run_logs(
        &self,
        owner: &str,
        repo: &str,
        run_id: i64,
    ) -> Result<(Mime, ByteStream)> {
        let req = Request::get(format!(
            "/repos/{}/{}/actions/runs/{}/logs",
            encode::seg(owner),
            encode::seg(repo),
            run_id
        ))
        .scope("read:repository")
        .accept(Accept::Any);
        self.client.bytes(req).await
    }
    /// Get a particular runner that belongs to the repository
    ///
    /// `GET /repos/{owner}/{repo}/actions/runners/{runner_id}` — needs the `read:repository` token scope.
    pub async fn get_repo_runner(
        &self,
        owner: &str,
        repo: &str,
        runner_id: &str,
    ) -> Result<forgejo_model::ActionRunner> {
        let req = Request::get(format!(
            "/repos/{}/{}/actions/runners/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(runner_id)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get runners belonging to the repository
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/actions/runners` — needs the `read:repository` token scope.
    pub fn get_repo_runners(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::GetRepoRunnersQuery,
    ) -> ItemStream<forgejo_model::ActionRunner> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/actions/runners",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get runners belonging to the repository
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/actions/runners` — needs the `read:repository` token scope.
    pub async fn get_repo_runners_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::GetRepoRunnersQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::ActionRunner>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/actions/runners",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get a repo-level variable
    ///
    /// `GET /repos/{owner}/{repo}/actions/variables/{variablename}` — needs the `read:repository` token scope.
    pub async fn get_repo_variable(
        &self,
        owner: &str,
        repo: &str,
        variablename: &str,
    ) -> Result<forgejo_model::ActionVariable> {
        let req = Request::get(format!(
            "/repos/{}/{}/actions/variables/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(variablename)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get repo-level variables list
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/actions/variables` — needs the `read:repository` token scope.
    pub fn get_repo_variables_list(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::GetRepoVariablesListQuery,
    ) -> ItemStream<forgejo_model::ActionVariable> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/actions/variables",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get repo-level variables list
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/actions/variables` — needs the `read:repository` token scope.
    pub async fn get_repo_variables_list_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::GetRepoVariablesListQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::ActionVariable>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/actions/variables",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get a repository's runner registration token This operation has been deprecated in Forgejo 15.
    /// Use the web UI or \[`/repos/{owner}/{repo}/actions/runners`\](#/repository/registerRepoRunner)
    /// instead.
    ///
    /// `GET /repos/{owner}/{repo}/actions/runners/registration-token` — needs the `read:repository` token scope.
    #[deprecated(
        note = "deprecated by the Forgejo API; see the endpoint's documentation for the replacement"
    )]
    pub async fn get_runner_registration_token(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<forgejo_model::RegistrationToken> {
        let req = Request::get(format!(
            "/repos/{}/{}/actions/runners/registration-token",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// List an repo's actions secrets
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/actions/secrets` — needs the `read:repository` token scope.
    pub fn list_actions_secrets(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListActionsSecretsQuery,
    ) -> ItemStream<forgejo_model::Secret> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/actions/secrets",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List an repo's actions secrets
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/actions/secrets` — needs the `read:repository` token scope.
    pub async fn list_actions_secrets_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListActionsSecretsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Secret>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/actions/secrets",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Register a new repository-level runner
    ///
    /// `POST /repos/{owner}/{repo}/actions/runners` — needs the `write:repository` token scope.
    pub async fn register_repo_runner(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::RegisterRunnerOptions,
    ) -> Result<forgejo_model::RegisterRunnerResponse> {
        let req = Request::post(format!(
            "/repos/{}/{}/actions/runners",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Search for repository's action jobs according filter conditions
    ///
    /// `GET /repos/{owner}/{repo}/actions/runners/jobs` — needs the `read:repository` token scope.
    pub async fn search_run_jobs(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoSearchRunJobsQuery,
    ) -> Result<Vec<forgejo_model::ActionRunJob>> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/actions/runners/jobs",
                encode::seg(owner),
                encode::seg(repo)
            ))
            .scope("read:repository"),
        );
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::ActionRunJob>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Create or Update a secret value in a repository
    ///
    /// `PUT /repos/{owner}/{repo}/actions/secrets/{secretname}` — needs the `write:repository` token scope.
    pub async fn update_repo_secret(
        &self,
        owner: &str,
        repo: &str,
        secretname: &str,
        body: &forgejo_model::CreateOrUpdateSecretOption,
    ) -> Result<()> {
        let req = Request::put(format!(
            "/repos/{}/{}/actions/secrets/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(secretname)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.empty(req).await
    }
    /// Update a repo-level variable
    ///
    /// `PUT /repos/{owner}/{repo}/actions/variables/{variablename}` — needs the `write:repository` token scope.
    pub async fn update_repo_variable(
        &self,
        owner: &str,
        repo: &str,
        variablename: &str,
        body: &forgejo_model::UpdateVariableOption,
    ) -> Result<()> {
        let req = Request::put(format!(
            "/repos/{}/{}/actions/variables/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(variablename)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.empty(req).await
    }
}
