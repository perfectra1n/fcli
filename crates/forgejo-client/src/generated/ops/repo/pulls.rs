// @generated by `cargo xtask codegen` from spec/forgejo-16.0.5.json
// (sha256:6d3563a56631a92b). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `repo` operations: `pulls`.
use forgejo_core::Result;
use forgejo_core::http::{Accept, ItemStream, PageInfo, Paging, Request, encode};
impl super::Repo<'_> {
    /// Cancel the scheduled auto merge for the given pull request
    ///
    /// `DELETE /repos/{owner}/{repo}/pulls/{index}/merge` — needs the `write:repository` token scope.
    pub async fn cancel_scheduled_auto_merge(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/pulls/{}/merge",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Create a pull request
    ///
    /// `POST /repos/{owner}/{repo}/pulls` — needs the `write:repository` token scope.
    pub async fn create_pull_request(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::CreatePullRequestOption,
    ) -> Result<forgejo_model::PullRequest> {
        let req =
            Request::post(format!("/repos/{}/{}/pulls", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a review to an pull request
    ///
    /// `POST /repos/{owner}/{repo}/pulls/{index}/reviews` — needs the `write:repository` token scope.
    pub async fn create_pull_review(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &forgejo_model::CreatePullReviewOptions,
    ) -> Result<forgejo_model::PullReview> {
        let req = Request::post(format!(
            "/repos/{}/{}/pulls/{}/reviews",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Add a new comment to a pull request review
    ///
    /// `POST /repos/{owner}/{repo}/pulls/{index}/reviews/{id}/comments` — needs the `write:repository` token scope.
    pub async fn create_pull_review_comment(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        id: i64,
        body: &forgejo_model::CreatePullReviewComment,
    ) -> Result<forgejo_model::PullReviewComment> {
        let req = Request::post(format!(
            "/repos/{}/{}/pulls/{}/reviews/{}/comments",
            encode::seg(owner),
            encode::seg(repo),
            index,
            id
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Create review requests for a pull request
    ///
    /// `POST /repos/{owner}/{repo}/pulls/{index}/requested_reviewers` — needs the `write:repository` token scope.
    pub async fn create_pull_review_requests(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &forgejo_model::PullReviewRequestOptions,
    ) -> Result<Vec<forgejo_model::PullReview>> {
        let req = Request::post(format!(
            "/repos/{}/{}/pulls/{}/requested_reviewers",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:repository")
        .json_body(body)?;
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::PullReview>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Delete a specific review from a pull request
    ///
    /// `DELETE /repos/{owner}/{repo}/pulls/{index}/reviews/{id}` — needs the `write:repository` token scope.
    pub async fn delete_pull_review(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        id: i64,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/pulls/{}/reviews/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            id
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a pull review comment
    ///
    /// `DELETE /repos/{owner}/{repo}/pulls/{index}/reviews/{id}/comments/{comment}` — needs the `write:repository` token scope.
    pub async fn delete_pull_review_comment(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        id: i64,
        comment: i64,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/pulls/{}/reviews/{}/comments/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            id,
            comment
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Cancel review requests for a pull request
    ///
    /// `DELETE /repos/{owner}/{repo}/pulls/{index}/requested_reviewers` — needs the `write:repository` token scope.
    pub async fn delete_pull_review_requests(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &forgejo_model::PullReviewRequestOptions,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/pulls/{}/requested_reviewers",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.empty(req).await
    }
    /// Dismiss a review for a pull request
    ///
    /// `POST /repos/{owner}/{repo}/pulls/{index}/reviews/{id}/dismissals` — needs the `write:repository` token scope.
    pub async fn dismiss_pull_review(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        id: i64,
        body: &forgejo_model::DismissPullReviewOptions,
    ) -> Result<forgejo_model::PullReview> {
        let req = Request::post(format!(
            "/repos/{}/{}/pulls/{}/reviews/{}/dismissals",
            encode::seg(owner),
            encode::seg(repo),
            index,
            id
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Get a pull request diff or patch
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{index}.{diffType}` — needs the `read:repository` token scope.
    pub async fn download_pull_diff_or_patch(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        diff_type: &str,
        query: &crate::query::RepoDownloadPullDiffOrPatchQuery,
    ) -> Result<String> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/pulls/{}.{}",
                encode::seg(owner),
                encode::seg(repo),
                index,
                encode::seg(diff_type)
            ))
            .scope("read:repository")
            .accept(Accept::Text),
        );
        crate::text(self.client, req).await
    }
    /// Update a pull request. If using deadline only the date will be taken into account, and time of
    /// day ignored.
    ///
    /// `PATCH /repos/{owner}/{repo}/pulls/{index}` — needs the `write:repository` token scope.
    pub async fn edit_pull_request(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &forgejo_model::EditPullRequestOption,
    ) -> Result<forgejo_model::PullRequest> {
        let req = Request::patch(format!(
            "/repos/{}/{}/pulls/{}",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Get a pull request
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{index}` — needs the `read:repository` token scope.
    pub async fn get_pull_request(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
    ) -> Result<forgejo_model::PullRequest> {
        let req = Request::get(format!(
            "/repos/{}/{}/pulls/{}",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get a pull request by base and head
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{base}/{head}` — needs the `read:repository` token scope.
    pub async fn get_pull_request_by_base_head(
        &self,
        owner: &str,
        repo: &str,
        base: &str,
        head: &str,
    ) -> Result<forgejo_model::PullRequest> {
        let req = Request::get(format!(
            "/repos/{}/{}/pulls/{}/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(base),
            encode::seg(head)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get commits for a pull request
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{index}/commits` — needs the `read:repository` token scope.
    pub fn get_pull_request_commits(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::RepoGetPullRequestCommitsQuery,
    ) -> ItemStream<forgejo_model::Commit> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/pulls/{}/commits",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get commits for a pull request
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{index}/commits` — needs the `read:repository` token scope.
    pub async fn get_pull_request_commits_page(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::RepoGetPullRequestCommitsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Commit>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/pulls/{}/commits",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get changed files for a pull request
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{index}/files` — needs the `read:repository` token scope.
    pub fn get_pull_request_files(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::RepoGetPullRequestFilesQuery,
    ) -> ItemStream<forgejo_model::ChangedFile> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/pulls/{}/files",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get changed files for a pull request
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{index}/files` — needs the `read:repository` token scope.
    pub async fn get_pull_request_files_page(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::RepoGetPullRequestFilesQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::ChangedFile>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/pulls/{}/files",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get a specific review for a pull request
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{index}/reviews/{id}` — needs the `read:repository` token scope.
    pub async fn get_pull_review(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        id: i64,
    ) -> Result<forgejo_model::PullReview> {
        let req = Request::get(format!(
            "/repos/{}/{}/pulls/{}/reviews/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            id
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get a pull review comment
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{index}/reviews/{id}/comments/{comment}` — needs the `read:repository` token scope.
    pub async fn get_pull_review_comment(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        id: i64,
        comment: i64,
    ) -> Result<forgejo_model::PullReviewComment> {
        let req = Request::get(format!(
            "/repos/{}/{}/pulls/{}/reviews/{}/comments/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            id,
            comment
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get a specific review for a pull request
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{index}/reviews/{id}/comments` — needs the `read:repository` token scope.
    pub async fn get_pull_review_comments(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        id: i64,
    ) -> Result<Vec<forgejo_model::PullReviewComment>> {
        let req = Request::get(format!(
            "/repos/{}/{}/pulls/{}/reviews/{}/comments",
            encode::seg(owner),
            encode::seg(repo),
            index,
            id
        ))
        .scope("read:repository");
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::PullReviewComment>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// List a repo's pinned pull requests
    ///
    /// `GET /repos/{owner}/{repo}/pulls/pinned` — needs the `read:repository` token scope.
    pub async fn list_pinned_pull_requests(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<Vec<forgejo_model::PullRequest>> {
        let req = Request::get(format!(
            "/repos/{}/{}/pulls/pinned",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("read:repository");
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::PullRequest>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// List a repo's pull requests. If a pull request is selected but fails to be retrieved for any
    /// reason, it will be a null value in the list of results.
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/pulls` — needs the `read:repository` token scope.
    pub fn list_pull_requests(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListPullRequestsQuery,
    ) -> ItemStream<forgejo_model::PullRequest> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/pulls", encode::seg(owner), encode::seg(repo)))
                    .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a repo's pull requests. If a pull request is selected but fails to be retrieved for any
    /// reason, it will be a null value in the list of results.
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/pulls` — needs the `read:repository` token scope.
    pub async fn list_pull_requests_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListPullRequestsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::PullRequest>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/pulls", encode::seg(owner), encode::seg(repo)))
                    .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List all reviews for a pull request
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{index}/reviews` — needs the `read:repository` token scope.
    pub fn list_pull_reviews(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::RepoListPullReviewsQuery,
    ) -> ItemStream<forgejo_model::PullReview> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/pulls/{}/reviews",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List all reviews for a pull request
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{index}/reviews` — needs the `read:repository` token scope.
    pub async fn list_pull_reviews_page(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::RepoListPullReviewsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::PullReview>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/pulls/{}/reviews",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Merge a pull request
    ///
    /// `POST /repos/{owner}/{repo}/pulls/{index}/merge` — needs the `write:repository` token scope.
    pub async fn merge_pull_request(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &forgejo_model::MergePullRequestOption,
    ) -> Result<()> {
        let req = Request::post(format!(
            "/repos/{}/{}/pulls/{}/merge",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.empty(req).await
    }
    /// Check if a pull request has been merged
    ///
    /// `GET /repos/{owner}/{repo}/pulls/{index}/merge` — needs the `read:repository` token scope.
    pub async fn pull_request_is_merged(&self, owner: &str, repo: &str, index: i64) -> Result<()> {
        let req = Request::get(format!(
            "/repos/{}/{}/pulls/{}/merge",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("read:repository");
        self.client.empty(req).await
    }
    /// Submit a pending review to an pull request
    ///
    /// `POST /repos/{owner}/{repo}/pulls/{index}/reviews/{id}` — needs the `write:repository` token scope.
    pub async fn submit_pull_review(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        id: i64,
        body: &forgejo_model::SubmitPullReviewOptions,
    ) -> Result<forgejo_model::PullReview> {
        let req = Request::post(format!(
            "/repos/{}/{}/pulls/{}/reviews/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            id
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Cancel to dismiss a review for a pull request
    ///
    /// `POST /repos/{owner}/{repo}/pulls/{index}/reviews/{id}/undismissals` — needs the `write:repository` token scope.
    pub async fn un_dismiss_pull_review(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        id: i64,
    ) -> Result<forgejo_model::PullReview> {
        let req = Request::post(format!(
            "/repos/{}/{}/pulls/{}/reviews/{}/undismissals",
            encode::seg(owner),
            encode::seg(repo),
            index,
            id
        ))
        .scope("write:repository");
        self.client.json(req).await
    }
    /// Merge PR's baseBranch into headBranch
    ///
    /// `POST /repos/{owner}/{repo}/pulls/{index}/update` — needs the `write:repository` token scope.
    pub async fn update_pull_request(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::RepoUpdatePullRequestQuery,
    ) -> Result<()> {
        let req = query.apply(
            Request::post(format!(
                "/repos/{}/{}/pulls/{}/update",
                encode::seg(owner),
                encode::seg(repo),
                index
            ))
            .scope("write:repository"),
        );
        self.client.empty(req).await
    }
}
