// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `repo` operations: `misc`.
use forgejo_core::Result;
use forgejo_core::http::{Accept, ByteStream, ItemStream, Mime, PageInfo, Paging, Request, encode};
impl super::Repo<'_> {
    /// Accept a repo transfer
    ///
    /// `POST /repos/{owner}/{repo}/transfer/accept` — needs the `write:repository` token scope.
    pub async fn accept_repo_transfer(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<forgejo_model::Repository> {
        let req = Request::post(format!(
            "/repos/{}/{}/transfer/accept",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:repository");
        self.client.json(req).await
    }
    /// Add a flag to a repository
    ///
    /// `PUT /repos/{owner}/{repo}/flags/{flag}` — needs the `write:repository` token scope.
    pub async fn add_flag(&self, owner: &str, repo: &str, flag: &str) -> Result<()> {
        let req = Request::put(format!(
            "/repos/{}/{}/flags/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(flag)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Set up a new push mirror in a repository
    ///
    /// `POST /repos/{owner}/{repo}/push_mirrors` — needs the `write:repository` token scope.
    pub async fn add_push_mirror(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::CreatePushMirrorOption,
    ) -> Result<forgejo_model::PushMirror> {
        let req = Request::post(format!(
            "/repos/{}/{}/push_mirrors",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Apply diff patch to repository
    ///
    /// `POST /repos/{owner}/{repo}/diffpatch` — needs the `write:repository` token scope.
    pub async fn apply_diff_patch(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::UpdateFileOptions,
    ) -> Result<forgejo_model::FileResponse> {
        let req =
            Request::post(format!("/repos/{}/{}/diffpatch", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Check if a repository has a given flag
    ///
    /// `GET /repos/{owner}/{repo}/flags/{flag}` — needs the `read:repository` token scope.
    pub async fn check_flag(&self, owner: &str, repo: &str, flag: &str) -> Result<()> {
        let req = Request::get(format!(
            "/repos/{}/{}/flags/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(flag)
        ))
        .scope("read:repository");
        self.client.empty(req).await
    }
    /// Convert a mirror repo to a normal repo.
    ///
    /// `POST /repos/{owner}/{repo}/convert` — needs the `write:repository` token scope.
    pub async fn convert(&self, owner: &str, repo: &str) -> Result<forgejo_model::Repository> {
        let req =
            Request::post(format!("/repos/{}/{}/convert", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository");
        self.client.json(req).await
    }
    /// Create a repository
    ///
    /// `POST /user/repos` — needs the `write:repository` token scope.
    pub async fn create_current_user_repo(
        &self,
        body: &forgejo_model::CreateRepoOption,
    ) -> Result<forgejo_model::Repository> {
        let req = Request::post("/user/repos").scope("write:repository").json_body(body)?;
        self.client.json(req).await
    }
    /// Fork a repository
    ///
    /// `POST /repos/{owner}/{repo}/forks` — needs the `write:repository` token scope.
    pub async fn create_fork(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::CreateForkOption,
    ) -> Result<forgejo_model::Repository> {
        let req =
            Request::post(format!("/repos/{}/{}/forks", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a commit status
    ///
    /// `POST /repos/{owner}/{repo}/statuses/{sha}` — needs the `write:repository` token scope.
    pub async fn create_status(
        &self,
        owner: &str,
        repo: &str,
        sha: &str,
        body: &forgejo_model::CreateStatusOption,
    ) -> Result<forgejo_model::CommitStatus> {
        let req = Request::post(format!(
            "/repos/{}/{}/statuses/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(sha)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a tag protections for a repository
    ///
    /// `POST /repos/{owner}/{repo}/tag_protections` — needs the `write:repository` token scope.
    pub async fn create_tag_protection(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::CreateTagProtectionOption,
    ) -> Result<forgejo_model::TagProtection> {
        let req = Request::post(format!(
            "/repos/{}/{}/tag_protections",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Delete a repository
    ///
    /// `DELETE /repos/{owner}/{repo}` — needs the `write:repository` token scope.
    pub async fn delete(&self, owner: &str, repo: &str) -> Result<()> {
        let req = Request::delete(format!("/repos/{}/{}", encode::seg(owner), encode::seg(repo)))
            .scope("write:repository");
        self.client.empty(req).await
    }
    /// Remove all flags from a repository
    ///
    /// `DELETE /repos/{owner}/{repo}/flags` — needs the `write:repository` token scope.
    pub async fn delete_all_flags(&self, owner: &str, repo: &str) -> Result<()> {
        let req =
            Request::delete(format!("/repos/{}/{}/flags", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a repository's avatar
    ///
    /// `DELETE /repos/{owner}/{repo}/avatar` — needs the `write:repository` token scope.
    pub async fn delete_avatar(&self, owner: &str, repo: &str) -> Result<()> {
        let req =
            Request::delete(format!("/repos/{}/{}/avatar", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository");
        self.client.empty(req).await
    }
    /// Remove a flag from a repository
    ///
    /// `DELETE /repos/{owner}/{repo}/flags/{flag}` — needs the `write:repository` token scope.
    pub async fn delete_flag(&self, owner: &str, repo: &str, flag: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/flags/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(flag)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Remove a push mirror from a repository by remoteName
    ///
    /// `DELETE /repos/{owner}/{repo}/push_mirrors/{name}` — needs the `write:repository` token scope.
    pub async fn delete_push_mirror(&self, owner: &str, repo: &str, name: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/push_mirrors/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(name)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a specific tag protection for the repository
    ///
    /// `DELETE /repos/{owner}/{repo}/tag_protections/{id}` — needs the `write:repository` token scope.
    pub async fn delete_tag_protection(&self, owner: &str, repo: &str, id: i64) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/tag_protections/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Edit a repository's properties. Only fields that are set will be changed.
    ///
    /// `PATCH /repos/{owner}/{repo}` — needs the `write:repository` token scope.
    pub async fn edit(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::EditRepoOption,
    ) -> Result<forgejo_model::Repository> {
        let req = Request::patch(format!("/repos/{}/{}", encode::seg(owner), encode::seg(repo)))
            .scope("write:repository")
            .json_body(body)?;
        self.client.json(req).await
    }
    /// Edit a tag protections for a repository. Only fields that are set will be changed
    ///
    /// `PATCH /repos/{owner}/{repo}/tag_protections/{id}` — needs the `write:repository` token scope.
    pub async fn edit_tag_protection(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        body: &forgejo_model::EditTagProtectionOption,
    ) -> Result<forgejo_model::TagProtection> {
        let req = Request::patch(format!(
            "/repos/{}/{}/tag_protections/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a repository using a template
    ///
    /// `POST /repos/{template_owner}/{template_repo}/generate` — needs the `write:repository` token scope.
    pub async fn generate_repo(
        &self,
        template_owner: &str,
        template_repo: &str,
        body: &forgejo_model::GenerateRepoOption,
    ) -> Result<forgejo_model::Repository> {
        let req = Request::post(format!(
            "/repos/{}/{}/generate",
            encode::seg(template_owner),
            encode::seg(template_repo)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Get a repository
    ///
    /// `GET /repos/{owner}/{repo}` — needs the `read:repository` token scope.
    pub async fn get(&self, owner: &str, repo: &str) -> Result<forgejo_model::Repository> {
        let req = Request::get(format!("/repos/{}/{}", encode::seg(owner), encode::seg(repo)))
            .scope("read:repository");
        self.client.json(req).await
    }
    /// Get an archive of a repository
    ///
    /// `GET /repos/{owner}/{repo}/archive/{archive}` — needs the `read:repository` token scope.
    pub async fn get_archive(
        &self,
        owner: &str,
        repo: &str,
        archive: &str,
    ) -> Result<(Mime, ByteStream)> {
        let req = Request::get(format!(
            "/repos/{}/{}/archive/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(archive)
        ))
        .scope("read:repository")
        .accept(Accept::Any);
        self.client.bytes(req).await
    }
    /// Get a repository by id
    ///
    /// `GET /repositories/{id}` — needs the `read:repository` token scope.
    pub async fn get_by_id(&self, id: i64) -> Result<forgejo_model::Repository> {
        let req = Request::get(format!("/repositories/{}", id)).scope("read:repository");
        self.client.json(req).await
    }
    /// Get the EditorConfig definitions of a file in a repository
    ///
    /// `GET /repos/{owner}/{repo}/editorconfig/{filepath}` — needs the `read:repository` token scope.
    pub async fn get_editor_config(
        &self,
        owner: &str,
        repo: &str,
        filepath: &str,
        query: &crate::query::RepoGetEditorConfigQuery,
    ) -> Result<::std::collections::BTreeMap<String, String>> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/editorconfig/{}",
                encode::seg(owner),
                encode::seg(repo),
                encode::path_like(filepath)
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Returns the issue config for a repo
    ///
    /// `GET /repos/{owner}/{repo}/issue_config` — needs the `read:repository` token scope.
    pub async fn get_issue_config(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<forgejo_model::IssueConfig> {
        let req = Request::get(format!(
            "/repos/{}/{}/issue_config",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get available issue templates for a repository
    ///
    /// `GET /repos/{owner}/{repo}/issue_templates` — needs the `read:repository` token scope.
    pub async fn get_issue_templates(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<Vec<forgejo_model::IssueTemplate>> {
        let req = Request::get(format!(
            "/repos/{}/{}/issue_templates",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("read:repository");
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::IssueTemplate>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Get languages and number of bytes of code written
    ///
    /// `GET /repos/{owner}/{repo}/languages` — needs the `read:repository` token scope.
    pub async fn get_languages(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<::std::collections::BTreeMap<String, i64>> {
        let req =
            Request::get(format!("/repos/{}/{}/languages", encode::seg(owner), encode::seg(repo)))
                .scope("read:repository");
        self.client.json(req).await
    }
    /// Get push mirror of the repository by remoteName
    ///
    /// `GET /repos/{owner}/{repo}/push_mirrors/{name}` — needs the `read:repository` token scope.
    pub async fn get_push_mirror_by_remote_name(
        &self,
        owner: &str,
        repo: &str,
        name: &str,
    ) -> Result<forgejo_model::PushMirror> {
        let req = Request::get(format!(
            "/repos/{}/{}/push_mirrors/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(name)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get a specific tag protection for the repository
    ///
    /// `GET /repos/{owner}/{repo}/tag_protections/{id}` — needs the `read:repository` token scope.
    pub async fn get_tag_protection(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
    ) -> Result<forgejo_model::TagProtection> {
        let req = Request::get(format!(
            "/repos/{}/{}/tag_protections/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// List a repository's activity feeds
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/activities/feeds` — needs the `read:repository` token scope.
    pub fn list_activity_feeds(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListActivityFeedsQuery,
    ) -> ItemStream<forgejo_model::Activity> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/activities/feeds",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a repository's activity feeds
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/activities/feeds` — needs the `read:repository` token scope.
    pub async fn list_activity_feeds_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListActivityFeedsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Activity>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/activities/feeds",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List a repository's flags
    ///
    /// `GET /repos/{owner}/{repo}/flags` — needs the `read:repository` token scope.
    pub async fn list_flags(&self, owner: &str, repo: &str) -> Result<Vec<String>> {
        let req =
            Request::get(format!("/repos/{}/{}/flags", encode::seg(owner), encode::seg(repo)))
                .scope("read:repository");
        Ok(self.client.json::<Option<Vec<String>>>(req).await?.unwrap_or_default())
    }
    /// List a repository's forks
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/forks` — needs the `read:repository` token scope.
    pub fn list_forks(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::ListForksQuery,
    ) -> ItemStream<forgejo_model::Repository> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/forks", encode::seg(owner), encode::seg(repo)))
                    .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a repository's forks
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/forks` — needs the `read:repository` token scope.
    pub async fn list_forks_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::ListForksQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Repository>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/forks", encode::seg(owner), encode::seg(repo)))
                    .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get all push mirrors of the repository
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/push_mirrors` — needs the `read:repository` token scope.
    pub fn list_push_mirrors(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListPushMirrorsQuery,
    ) -> ItemStream<forgejo_model::PushMirror> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/push_mirrors",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get all push mirrors of the repository
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/push_mirrors` — needs the `read:repository` token scope.
    pub async fn list_push_mirrors_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListPushMirrorsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::PushMirror>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/push_mirrors",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List a repo's stargazers
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/stargazers` — needs the `read:repository` token scope.
    pub fn list_stargazers(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListStargazersQuery,
    ) -> ItemStream<forgejo_model::User> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/stargazers",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a repo's stargazers
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/stargazers` — needs the `read:repository` token scope.
    pub async fn list_stargazers_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListStargazersQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/stargazers",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get a commit's statuses
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/statuses/{sha}` — needs the `read:repository` token scope.
    pub fn list_statuses(
        &self,
        owner: &str,
        repo: &str,
        sha: &str,
        query: &crate::query::RepoListStatusesQuery,
    ) -> ItemStream<forgejo_model::CommitStatus> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/statuses/{}",
                    encode::seg(owner),
                    encode::seg(repo),
                    encode::seg(sha)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get a commit's statuses
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/statuses/{sha}` — needs the `read:repository` token scope.
    pub async fn list_statuses_page(
        &self,
        owner: &str,
        repo: &str,
        sha: &str,
        query: &crate::query::RepoListStatusesQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::CommitStatus>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/statuses/{}",
                    encode::seg(owner),
                    encode::seg(repo),
                    encode::seg(sha)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List tag protections for a repository
    ///
    /// `GET /repos/{owner}/{repo}/tag_protections` — needs the `read:repository` token scope.
    pub async fn list_tag_protection(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<Vec<forgejo_model::TagProtection>> {
        let req = Request::get(format!(
            "/repos/{}/{}/tag_protections",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("read:repository");
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::TagProtection>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Migrate a remote git repository
    ///
    /// `POST /repos/migrate` — needs the `write:repository` token scope.
    pub async fn migrate(
        &self,
        body: &forgejo_model::MigrateRepoOptions,
    ) -> Result<forgejo_model::Repository> {
        let req = Request::post("/repos/migrate").scope("write:repository").json_body(body)?;
        self.client.json(req).await
    }
    /// Sync a mirrored repository
    ///
    /// `POST /repos/{owner}/{repo}/mirror-sync` — needs the `write:repository` token scope.
    pub async fn mirror_sync(&self, owner: &str, repo: &str) -> Result<()> {
        let req = Request::post(format!(
            "/repos/{}/{}/mirror-sync",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Returns if new Issue Pins are allowed
    ///
    /// `GET /repos/{owner}/{repo}/new_pin_allowed` — needs the `read:repository` token scope.
    pub async fn new_pin_allowed(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<forgejo_model::NewIssuePinsAllowed> {
        let req = Request::get(format!(
            "/repos/{}/{}/new_pin_allowed",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Sync all push mirrored repository
    ///
    /// `POST /repos/{owner}/{repo}/push_mirrors-sync` — needs the `write:repository` token scope.
    pub async fn push_mirror_sync(&self, owner: &str, repo: &str) -> Result<()> {
        let req = Request::post(format!(
            "/repos/{}/{}/push_mirrors-sync",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Reject a repo transfer
    ///
    /// `POST /repos/{owner}/{repo}/transfer/reject` — needs the `write:repository` token scope.
    pub async fn reject_repo_transfer(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<forgejo_model::Repository> {
        let req = Request::post(format!(
            "/repos/{}/{}/transfer/reject",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:repository");
        self.client.json(req).await
    }
    /// Replace all flags of a repository
    ///
    /// `PUT /repos/{owner}/{repo}/flags` — needs the `write:repository` token scope.
    pub async fn replace_all_flags(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::ReplaceFlagsOption,
    ) -> Result<()> {
        let req =
            Request::put(format!("/repos/{}/{}/flags", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.empty(req).await
    }
    /// Search for repositories
    ///
    /// `GET /repos/search` — needs the `read:repository` token scope.
    pub async fn search(
        &self,
        query: &crate::query::RepoSearchQuery,
    ) -> Result<forgejo_model::SearchResults> {
        let req = query.apply(Request::get("/repos/search").scope("read:repository"));
        self.client.json(req).await
    }
    /// Get signing-key.gpg for given repository
    ///
    /// `GET /repos/{owner}/{repo}/signing-key.gpg` — needs the `read:repository` token scope.
    pub async fn signing_key(&self, owner: &str, repo: &str) -> Result<String> {
        let req = Request::get(format!(
            "/repos/{}/{}/signing-key.gpg",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("read:repository")
        .accept(Accept::Text);
        crate::text(self.client, req).await
    }
    /// Syncs a fork branch with the base branch
    ///
    /// `POST /repos/{owner}/{repo}/sync_fork/{branch}` — needs the `write:repository` token scope.
    pub async fn sync_fork_branch(&self, owner: &str, repo: &str, branch: &str) -> Result<()> {
        let req = Request::post(format!(
            "/repos/{}/{}/sync_fork/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(branch)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Gets information about syncing a fork branch with the base branch
    ///
    /// `GET /repos/{owner}/{repo}/sync_fork/{branch}` — needs the `read:repository` token scope.
    pub async fn sync_fork_branch_info(
        &self,
        owner: &str,
        repo: &str,
        branch: &str,
    ) -> Result<forgejo_model::SyncForkInfo> {
        let req = Request::get(format!(
            "/repos/{}/{}/sync_fork/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(branch)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Syncs the default branch of a fork with the base branch
    ///
    /// `POST /repos/{owner}/{repo}/sync_fork` — needs the `write:repository` token scope.
    pub async fn sync_fork_default(&self, owner: &str, repo: &str) -> Result<()> {
        let req =
            Request::post(format!("/repos/{}/{}/sync_fork", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository");
        self.client.empty(req).await
    }
    /// Gets information about syncing the fork default branch with the base branch
    ///
    /// `GET /repos/{owner}/{repo}/sync_fork` — needs the `read:repository` token scope.
    pub async fn sync_fork_default_info(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<forgejo_model::SyncForkInfo> {
        let req =
            Request::get(format!("/repos/{}/{}/sync_fork", encode::seg(owner), encode::seg(repo)))
                .scope("read:repository");
        self.client.json(req).await
    }
    /// List a repo's tracked times
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/times` — needs the `read:repository` token scope.
    pub fn tracked_times(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoTrackedTimesQuery,
    ) -> ItemStream<forgejo_model::TrackedTime> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/times", encode::seg(owner), encode::seg(repo)))
                    .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a repo's tracked times
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/times` — needs the `read:repository` token scope.
    pub async fn tracked_times_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoTrackedTimesQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::TrackedTime>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/times", encode::seg(owner), encode::seg(repo)))
                    .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Transfer a repo ownership
    ///
    /// `POST /repos/{owner}/{repo}/transfer` — needs the `write:repository` token scope.
    pub async fn transfer(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::TransferRepoOption,
    ) -> Result<forgejo_model::Repository> {
        let req =
            Request::post(format!("/repos/{}/{}/transfer", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Update a repository's avatar
    ///
    /// `POST /repos/{owner}/{repo}/avatar` — needs the `write:repository` token scope.
    pub async fn update_avatar(
        &self,
        owner: &str,
        repo: &str,
        body: &forgejo_model::UpdateRepoAvatarOption,
    ) -> Result<()> {
        let req =
            Request::post(format!("/repos/{}/{}/avatar", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.empty(req).await
    }
    /// Returns the validation information for a issue config
    ///
    /// `GET /repos/{owner}/{repo}/issue_config/validate` — needs the `read:repository` token scope.
    pub async fn validate_issue_config(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<forgejo_model::IssueConfigValidation> {
        let req = Request::get(format!(
            "/repos/{}/{}/issue_config/validate",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
}
