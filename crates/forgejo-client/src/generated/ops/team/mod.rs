// @generated by `cargo xtask codegen` from spec/forgejo-16.0.5.json
// (sha256:6d3563a56631a92b). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `team` — Teams, independent of their organization.
use forgejo_core::Result;
use forgejo_core::http::{Client, Request, encode};
/// Teams, independent of their organization.
///
/// Obtain one from `Api::team`, or wrap a bare `Client` with `Team::new`.
#[derive(Debug, Clone, Copy)]
pub struct Team<'a> {
    pub(crate) client: &'a Client,
}
impl<'a> Team<'a> {
    /// Borrow a client as this operation group. Free — no allocation, no clone.
    pub fn new(client: &'a Client) -> Self {
        Self { client }
    }
    /// Search for teams within an organization
    ///
    /// `GET /orgs/{org}/teams/search` — needs the `read:organization` token scope.
    pub async fn search(
        &self,
        org: &str,
        query: &crate::query::TeamSearchQuery,
    ) -> Result<forgejo_model::TeamSearchResponse> {
        let req = query.apply(
            Request::get(format!("/orgs/{}/teams/search", encode::seg(org)))
                .scope("read:organization"),
        );
        self.client.json(req).await
    }
}
