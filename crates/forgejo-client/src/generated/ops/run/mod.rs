// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `run` — Actions runs.
use forgejo_core::Result;
use forgejo_core::http::{Client, ItemStream, PageInfo, Paging, Request, encode};
/// Actions runs.
///
/// Obtain one from `Api::run`, or wrap a bare `Client` with `Run::new`.
#[derive(Debug, Clone, Copy)]
pub struct Run<'a> {
    pub(crate) client: &'a Client,
}
impl<'a> Run<'a> {
    /// Borrow a client as this operation group. Free — no allocation, no clone.
    pub fn new(client: &'a Client) -> Self {
        Self { client }
    }
    /// List artifacts of a workflow run
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/actions/runs/{run_id}/artifacts` — needs the `read:repository` token scope.
    pub fn artifacts(
        &self,
        owner: &str,
        repo: &str,
        run_id: i64,
        query: &crate::query::ListActionRunArtifactsQuery,
    ) -> ItemStream<forgejo_model::ActionArtifact> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/actions/runs/{}/artifacts",
                    encode::seg(owner),
                    encode::seg(repo),
                    run_id
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List artifacts of a workflow run
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/actions/runs/{run_id}/artifacts` — needs the `read:repository` token scope.
    pub async fn artifacts_page(
        &self,
        owner: &str,
        repo: &str,
        run_id: i64,
        query: &crate::query::ListActionRunArtifactsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::ActionArtifact>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/actions/runs/{}/artifacts",
                    encode::seg(owner),
                    encode::seg(repo),
                    run_id
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Cancel a pending or running workflow run. Cancel a particular workflow run. Pending or running
    /// jobs of the run are cancelled. A run that has already finished, whether cancelled, failed,
    /// skipped or succeeded, is left unchanged. In both cases the endpoint responds with HTTP 204.
    ///
    /// `POST /repos/{owner}/{repo}/actions/runs/{run_id}/cancel` — needs the `write:repository` token scope.
    pub async fn cancel(&self, owner: &str, repo: &str, run_id: i64) -> Result<()> {
        let req = Request::post(format!(
            "/repos/{}/{}/actions/runs/{}/cancel",
            encode::seg(owner),
            encode::seg(repo),
            run_id
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a completed workflow run. Remove a particular workflow run. The workflow run must have
    /// completed (succeeded, failed, cancelled) for the operation to succeed. Otherwise, an error is
    /// returned.
    ///
    /// `DELETE /repos/{owner}/{repo}/actions/runs/{run_id}` — needs the `write:repository` token scope.
    pub async fn delete(&self, owner: &str, repo: &str, run_id: i64) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/actions/runs/{}",
            encode::seg(owner),
            encode::seg(repo),
            run_id
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// List jobs of a workflow run
    ///
    /// `GET /repos/{owner}/{repo}/actions/runs/{run_id}/jobs` — needs the `read:repository` token scope.
    pub async fn jobs(
        &self,
        owner: &str,
        repo: &str,
        run_id: i64,
    ) -> Result<Vec<forgejo_model::ActionRunJob>> {
        let req = Request::get(format!(
            "/repos/{}/{}/actions/runs/{}/jobs",
            encode::seg(owner),
            encode::seg(repo),
            run_id
        ))
        .scope("read:repository");
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::ActionRunJob>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// List a repository's action runs
    ///
    /// `GET /repos/{owner}/{repo}/actions/runs` — needs the `read:repository` token scope.
    pub async fn list(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::ListActionRunsQuery,
    ) -> Result<forgejo_model::ListActionRunResponse> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/actions/runs",
                encode::seg(owner),
                encode::seg(repo)
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Get an action run
    ///
    /// `GET /repos/{owner}/{repo}/actions/runs/{run_id}` — needs the `read:repository` token scope.
    pub async fn view(
        &self,
        owner: &str,
        repo: &str,
        run_id: i64,
    ) -> Result<forgejo_model::ActionRun> {
        let req = Request::get(format!(
            "/repos/{}/{}/actions/runs/{}",
            encode::seg(owner),
            encode::seg(repo),
            run_id
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
}
