// @generated by `cargo xtask codegen` from spec/forgejo-16.0.5.json
// (sha256:6d3563a56631a92b). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `notify` — Notification threads and read state.
use forgejo_core::Result;
use forgejo_core::http::{Client, ItemStream, PageInfo, Paging, Request, encode};
/// Notification threads and read state.
///
/// Obtain one from `Api::notify`, or wrap a bare `Client` with `Notify::new`.
#[derive(Debug, Clone, Copy)]
pub struct Notify<'a> {
    pub(crate) client: &'a Client,
}
impl<'a> Notify<'a> {
    /// Borrow a client as this operation group. Free — no allocation, no clone.
    pub fn new(client: &'a Client) -> Self {
        Self { client }
    }
    /// List users's notification threads
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /notifications` — needs the `read:notification` token scope.
    pub fn get_list(
        &self,
        query: &crate::query::NotifyGetListQuery,
    ) -> ItemStream<forgejo_model::NotificationThread> {
        let req = crate::stream_request(
            query.apply(Request::get("/notifications").scope("read:notification")),
            "page",
        );
        self.client.items(req)
    }
    /// List users's notification threads
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /notifications` — needs the `read:notification` token scope.
    pub async fn get_list_page(
        &self,
        query: &crate::query::NotifyGetListQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::NotificationThread>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/notifications").scope("read:notification")),
            paging,
        );
        self.client.page(req).await
    }
    /// List users's notification threads on a specific repo
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/notifications` — needs the `read:notification` token scope.
    pub fn get_repo_list(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::NotifyGetRepoListQuery,
    ) -> ItemStream<forgejo_model::NotificationThread> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/notifications",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:notification"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List users's notification threads on a specific repo
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/notifications` — needs the `read:notification` token scope.
    pub async fn get_repo_list_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::NotifyGetRepoListQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::NotificationThread>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/notifications",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:notification"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get notification thread by ID
    ///
    /// `GET /notifications/threads/{id}` — needs the `read:notification` token scope.
    pub async fn get_thread(&self, id: i64) -> Result<forgejo_model::NotificationThread> {
        let req = Request::get(format!("/notifications/threads/{}", id)).scope("read:notification");
        self.client.json(req).await
    }
    /// Check if unread notifications exist
    ///
    /// `GET /notifications/new` — needs the `read:notification` token scope.
    pub async fn new_available(&self) -> Result<forgejo_model::NotificationCount> {
        let req = Request::get("/notifications/new").scope("read:notification");
        self.client.json(req).await
    }
    /// Mark notification threads as read, pinned or unread
    ///
    /// `PUT /notifications` — needs the `write:notification` token scope.
    pub async fn read_list(
        &self,
        query: &crate::query::NotifyReadListQuery,
    ) -> Result<Vec<forgejo_model::NotificationThread>> {
        let req = query.apply(Request::put("/notifications").scope("write:notification"));
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::NotificationThread>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Mark notification threads as read, pinned or unread on a specific repo
    ///
    /// `PUT /repos/{owner}/{repo}/notifications` — needs the `write:notification` token scope.
    pub async fn read_repo_list(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::NotifyReadRepoListQuery,
    ) -> Result<Vec<forgejo_model::NotificationThread>> {
        let req = query.apply(
            Request::put(format!(
                "/repos/{}/{}/notifications",
                encode::seg(owner),
                encode::seg(repo)
            ))
            .scope("write:notification"),
        );
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::NotificationThread>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Mark notification thread as read by ID
    ///
    /// `PATCH /notifications/threads/{id}` — needs the `write:notification` token scope.
    pub async fn read_thread(
        &self,
        id: i64,
        query: &crate::query::NotifyReadThreadQuery,
    ) -> Result<forgejo_model::NotificationThread> {
        let req = query.apply(
            Request::patch(format!("/notifications/threads/{}", id)).scope("write:notification"),
        );
        self.client.json(req).await
    }
}
