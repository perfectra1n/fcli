// @generated by `cargo xtask codegen` from spec/forgejo-16.0.5.json
// (sha256:6d3563a56631a92b). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `workflow` — Actions workflows.
use forgejo_core::Result;
use forgejo_core::http::{Client, Request, encode};
/// Actions workflows.
///
/// Obtain one from `Api::workflow`, or wrap a bare `Client` with `Workflow::new`.
#[derive(Debug, Clone, Copy)]
pub struct Workflow<'a> {
    pub(crate) client: &'a Client,
}
impl<'a> Workflow<'a> {
    /// Borrow a client as this operation group. Free — no allocation, no clone.
    pub fn new(client: &'a Client) -> Self {
        Self { client }
    }
    /// Dispatches a workflow
    ///
    /// `POST /repos/{owner}/{repo}/actions/workflows/{workflowfilename}/dispatches` — needs the `write:repository` token scope.
    pub async fn dispatch(
        &self,
        owner: &str,
        repo: &str,
        workflowfilename: &str,
        body: &forgejo_model::DispatchWorkflowOption,
    ) -> Result<forgejo_model::DispatchWorkflowRun> {
        let req = Request::post(format!(
            "/repos/{}/{}/actions/workflows/{}/dispatches",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(workflowfilename)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
}
