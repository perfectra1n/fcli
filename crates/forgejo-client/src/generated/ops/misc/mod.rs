// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `misc` — Markdown rendering, version, signing key, gitignore and licence templates.
use forgejo_core::Result;
use forgejo_core::http::{Accept, Body, Client, Request, Source, encode};
/// Markdown rendering, version, signing key, gitignore and licence templates.
///
/// Obtain one from `Api::misc`, or wrap a bare `Client` with `Misc::new`.
#[derive(Debug, Clone, Copy)]
pub struct Misc<'a> {
    pub(crate) client: &'a Client,
}
impl<'a> Misc<'a> {
    /// Borrow a client as this operation group. Free — no allocation, no clone.
    pub fn new(client: &'a Client) -> Self {
        Self { client }
    }
    /// Get a workflow run associated with a token The automatic actions token must be used as the
    /// authentication mechanism (Authorization: Bearer ${{ forgejo.token }}); other types of tokens
    /// cannot be used. The token is associated with the job, which must be still running for the
    /// request to this endpoint to succeed.
    ///
    /// `GET /actions/run` — needs the `read:misc` token scope.
    pub async fn get_actions_run(&self) -> Result<forgejo_model::ActionRun> {
        let req = Request::get("/actions/run").scope("read:misc");
        self.client.json(req).await
    }
    /// Returns information about a gitignore template
    ///
    /// `GET /gitignore/templates/{name}` — needs the `read:misc` token scope.
    pub async fn get_gitignore_template_info(
        &self,
        name: &str,
    ) -> Result<forgejo_model::GitignoreTemplateInfo> {
        let req =
            Request::get(format!("/gitignore/templates/{}", encode::seg(name))).scope("read:misc");
        self.client.json(req).await
    }
    /// Returns all labels in a template
    ///
    /// `GET /label/templates/{name}` — needs the `read:misc` token scope.
    pub async fn get_label_template_info(
        &self,
        name: &str,
    ) -> Result<Vec<forgejo_model::LabelTemplate>> {
        let req =
            Request::get(format!("/label/templates/{}", encode::seg(name))).scope("read:misc");
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::LabelTemplate>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Returns information about a license template
    ///
    /// `GET /licenses/{name}` — needs the `read:misc` token scope.
    pub async fn get_license_template_info(
        &self,
        name: &str,
    ) -> Result<forgejo_model::LicenseTemplateInfo> {
        let req = Request::get(format!("/licenses/{}", encode::seg(name))).scope("read:misc");
        self.client.json(req).await
    }
    /// Returns the nodeinfo of the Forgejo application
    ///
    /// `GET /nodeinfo` — needs the `read:misc` token scope.
    pub async fn get_node_info(&self) -> Result<forgejo_model::NodeInfo> {
        let req = Request::get("/nodeinfo").scope("read:misc");
        self.client.json(req).await
    }
    /// Get default signing-key.gpg
    ///
    /// `GET /signing-key.gpg` — needs the `read:misc` token scope.
    pub async fn get_signing_key(&self) -> Result<String> {
        let req = Request::get("/signing-key.gpg").scope("read:misc").accept(Accept::Text);
        crate::text(self.client, req).await
    }
    /// Get default signing-key.ssh
    ///
    /// `GET /signing-key.ssh` — needs the `read:misc` token scope.
    pub async fn get_ssh_signing_key(&self) -> Result<String> {
        let req = Request::get("/signing-key.ssh").scope("read:misc").accept(Accept::Text);
        crate::text(self.client, req).await
    }
    /// Returns the version of the running application
    ///
    /// `GET /version` — needs the `read:misc` token scope.
    pub async fn get_version(&self) -> Result<forgejo_model::ServerVersion> {
        let req = Request::get("/version").scope("read:misc");
        self.client.json(req).await
    }
    /// Returns a list of all gitignore templates
    ///
    /// `GET /gitignore/templates` — needs the `read:misc` token scope.
    pub async fn list_gitignores_templates(&self) -> Result<Vec<String>> {
        let req = Request::get("/gitignore/templates").scope("read:misc");
        Ok(self.client.json::<Option<Vec<String>>>(req).await?.unwrap_or_default())
    }
    /// Returns a list of all label templates
    ///
    /// `GET /label/templates` — needs the `read:misc` token scope.
    pub async fn list_label_templates(&self) -> Result<Vec<String>> {
        let req = Request::get("/label/templates").scope("read:misc");
        Ok(self.client.json::<Option<Vec<String>>>(req).await?.unwrap_or_default())
    }
    /// Returns a list of all license templates
    ///
    /// `GET /licenses` — needs the `read:misc` token scope.
    pub async fn list_license_templates(
        &self,
    ) -> Result<Vec<forgejo_model::LicensesTemplateListEntry>> {
        let req = Request::get("/licenses").scope("read:misc");
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::LicensesTemplateListEntry>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Render a markdown document as HTML
    ///
    /// `POST /markdown` — needs the `write:misc` token scope.
    pub async fn render_markdown(&self, body: &forgejo_model::MarkdownOption) -> Result<String> {
        let req =
            Request::post("/markdown").scope("write:misc").accept(Accept::Html).json_body(body)?;
        crate::text(self.client, req).await
    }
    /// Render raw markdown as HTML
    ///
    /// `POST /markdown/raw` — needs the `write:misc` token scope.
    pub async fn render_markdown_raw(&self, body: &str) -> Result<String> {
        let req = Request::post("/markdown/raw").scope("write:misc").accept(Accept::Html).body(
            Body::Octets {
                mime: "text/plain".to_owned(),
                src: Source::Bytes(body.as_bytes().to_vec()),
            },
        );
        crate::text(self.client, req).await
    }
    /// Render a markup document as HTML
    ///
    /// `POST /markup` — needs the `write:misc` token scope.
    pub async fn render_markup(&self, body: &forgejo_model::MarkupOption) -> Result<String> {
        let req =
            Request::post("/markup").scope("write:misc").accept(Accept::Html).json_body(body)?;
        crate::text(self.client, req).await
    }
}
