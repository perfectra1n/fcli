// @generated by `cargo xtask codegen` from spec/forgejo-16.0.5.json
// (sha256:6d3563a56631a92b). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `org` operations: `c`.
use forgejo_core::Result;
use forgejo_core::http::{Request, encode};
impl super::Org<'_> {
    /// Check if the organization is over quota for a given subject
    ///
    /// `GET /orgs/{org}/quota/check` — needs the `read:organization` token scope.
    pub async fn check_quota(
        &self,
        org: &str,
        query: &crate::query::OrgCheckQuotaQuery,
    ) -> Result<bool> {
        let req = query.apply(
            Request::get(format!("/orgs/{}/quota/check", encode::seg(org)))
                .scope("read:organization"),
        );
        self.client.json(req).await
    }
    /// Conceal a user's membership
    ///
    /// `DELETE /orgs/{org}/public_members/{username}` — needs the `write:organization` token scope.
    pub async fn conceal_member(&self, org: &str, username: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/orgs/{}/public_members/{}",
            encode::seg(org),
            encode::seg(username)
        ))
        .scope("write:organization");
        self.client.empty(req).await
    }
    /// Create an organization
    ///
    /// `POST /orgs` — needs the `write:organization` token scope.
    pub async fn create(
        &self,
        body: &forgejo_model::CreateOrgOption,
    ) -> Result<forgejo_model::Organization> {
        let req = Request::post("/orgs").scope("write:organization").json_body(body)?;
        self.client.json(req).await
    }
    /// Create a hook
    ///
    /// `POST /orgs/{org}/hooks` — needs the `write:organization` token scope.
    pub async fn create_hook(
        &self,
        org: &str,
        body: &forgejo_model::CreateHookOption,
    ) -> Result<forgejo_model::Hook> {
        let req = Request::post(format!("/orgs/{}/hooks", encode::seg(org)))
            .scope("write:organization")
            .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a label for an organization
    ///
    /// `POST /orgs/{org}/labels` — needs the `write:organization` token scope.
    pub async fn create_label(
        &self,
        org: &str,
        body: &forgejo_model::CreateLabelOption,
    ) -> Result<forgejo_model::Label> {
        let req = Request::post(format!("/orgs/{}/labels", encode::seg(org)))
            .scope("write:organization")
            .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a repository in an organization
    ///
    /// `POST /orgs/{org}/repos` — needs the `write:organization` token scope.
    pub async fn create_org_repo(
        &self,
        org: &str,
        body: &forgejo_model::CreateRepoOption,
    ) -> Result<forgejo_model::Repository> {
        let req = Request::post(format!("/orgs/{}/repos", encode::seg(org)))
            .scope("write:organization")
            .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a repository in an organization
    ///
    /// `POST /org/{org}/repos` — needs the `write:organization` token scope.
    #[deprecated(
        note = "deprecated by the Forgejo API; see the endpoint's documentation for the replacement"
    )]
    pub async fn create_org_repo_deprecated(
        &self,
        org: &str,
        body: &forgejo_model::CreateRepoOption,
    ) -> Result<forgejo_model::Repository> {
        let req = Request::post(format!("/org/{}/repos", encode::seg(org)))
            .scope("write:organization")
            .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a new variable in organization
    ///
    /// `POST /orgs/{org}/actions/variables/{variablename}` — needs the `write:organization` token scope.
    pub async fn create_org_variable(
        &self,
        org: &str,
        variablename: &str,
        body: &forgejo_model::CreateVariableOption,
    ) -> Result<()> {
        let req = Request::post(format!(
            "/orgs/{}/actions/variables/{}",
            encode::seg(org),
            encode::seg(variablename)
        ))
        .scope("write:organization")
        .json_body(body)?;
        self.client.empty(req).await
    }
    /// Create a team
    ///
    /// `POST /orgs/{org}/teams` — needs the `write:organization` token scope.
    pub async fn create_team(
        &self,
        org: &str,
        body: &forgejo_model::CreateTeamOption,
    ) -> Result<forgejo_model::Team> {
        let req = Request::post(format!("/orgs/{}/teams", encode::seg(org)))
            .scope("write:organization")
            .json_body(body)?;
        self.client.json(req).await
    }
}
