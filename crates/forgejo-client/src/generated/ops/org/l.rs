// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `org` operations: `l`.
use forgejo_core::Result;
use forgejo_core::http::{ItemStream, PageInfo, Paging, Request, encode};
impl super::Org<'_> {
    /// List actions secrets of an organization
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/actions/secrets` — needs the `read:organization` token scope.
    pub fn list_actions_secrets(
        &self,
        org: &str,
        query: &crate::query::OrgListActionsSecretsQuery,
    ) -> ItemStream<forgejo_model::Secret> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/actions/secrets", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List actions secrets of an organization
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/actions/secrets` — needs the `read:organization` token scope.
    pub async fn list_actions_secrets_page(
        &self,
        org: &str,
        query: &crate::query::OrgListActionsSecretsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Secret>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/actions/secrets", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List an organization's activity feeds
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/activities/feeds` — needs the `read:organization` token scope.
    pub fn list_activity_feeds(
        &self,
        org: &str,
        query: &crate::query::OrgListActivityFeedsQuery,
    ) -> ItemStream<forgejo_model::Activity> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/activities/feeds", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List an organization's activity feeds
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/activities/feeds` — needs the `read:organization` token scope.
    pub async fn list_activity_feeds_page(
        &self,
        org: &str,
        query: &crate::query::OrgListActivityFeedsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Activity>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/activities/feeds", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List the organization's blocked users
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/list_blocked` — needs the `read:organization` token scope.
    pub fn list_blocked_users(
        &self,
        org: &str,
        query: &crate::query::OrgListBlockedUsersQuery,
    ) -> ItemStream<forgejo_model::BlockedUser> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/list_blocked", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the organization's blocked users
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/list_blocked` — needs the `read:organization` token scope.
    pub async fn list_blocked_users_page(
        &self,
        org: &str,
        query: &crate::query::OrgListBlockedUsersQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::BlockedUser>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/list_blocked", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List the current user's organizations
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/orgs` — needs the `read:organization` token scope.
    pub fn list_current_user_orgs(
        &self,
        query: &crate::query::OrgListCurrentUserOrgsQuery,
    ) -> ItemStream<forgejo_model::Organization> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/orgs").scope("read:organization")),
            "page",
        );
        self.client.items(req)
    }
    /// List the current user's organizations
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/orgs` — needs the `read:organization` token scope.
    pub async fn list_current_user_orgs_page(
        &self,
        query: &crate::query::OrgListCurrentUserOrgsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Organization>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/orgs").scope("read:organization")),
            paging,
        );
        self.client.page(req).await
    }
    /// List an organization's webhooks
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/hooks` — needs the `read:organization` token scope.
    pub fn list_hooks(
        &self,
        org: &str,
        query: &crate::query::OrgListHooksQuery,
    ) -> ItemStream<forgejo_model::Hook> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/hooks", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List an organization's webhooks
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/hooks` — needs the `read:organization` token scope.
    pub async fn list_hooks_page(
        &self,
        org: &str,
        query: &crate::query::OrgListHooksQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Hook>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/hooks", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List an organization's labels
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/labels` — needs the `read:organization` token scope.
    pub fn list_labels(
        &self,
        org: &str,
        query: &crate::query::OrgListLabelsQuery,
    ) -> ItemStream<forgejo_model::Label> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/labels", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List an organization's labels
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/labels` — needs the `read:organization` token scope.
    pub async fn list_labels_page(
        &self,
        org: &str,
        query: &crate::query::OrgListLabelsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Label>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/labels", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List an organization's members
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/members` — needs the `read:organization` token scope.
    pub fn list_members(
        &self,
        org: &str,
        query: &crate::query::OrgListMembersQuery,
    ) -> ItemStream<forgejo_model::User> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/members", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List an organization's members
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/members` — needs the `read:organization` token scope.
    pub async fn list_members_page(
        &self,
        org: &str,
        query: &crate::query::OrgListMembersQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/members", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List an organization's public members
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/public_members` — needs the `read:organization` token scope.
    pub fn list_public_members(
        &self,
        org: &str,
        query: &crate::query::OrgListPublicMembersQuery,
    ) -> ItemStream<forgejo_model::User> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/public_members", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List an organization's public members
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/public_members` — needs the `read:organization` token scope.
    pub async fn list_public_members_page(
        &self,
        org: &str,
        query: &crate::query::OrgListPublicMembersQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/public_members", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List the artifacts affecting the organization's quota
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/quota/artifacts` — needs the `read:organization` token scope.
    pub fn list_quota_artifacts(
        &self,
        org: &str,
        query: &crate::query::OrgListQuotaArtifactsQuery,
    ) -> ItemStream<forgejo_model::QuotaUsedArtifact> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/quota/artifacts", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the artifacts affecting the organization's quota
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/quota/artifacts` — needs the `read:organization` token scope.
    pub async fn list_quota_artifacts_page(
        &self,
        org: &str,
        query: &crate::query::OrgListQuotaArtifactsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::QuotaUsedArtifact>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/quota/artifacts", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List the attachments affecting the organization's quota
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/quota/attachments` — needs the `read:organization` token scope.
    pub fn list_quota_attachments(
        &self,
        org: &str,
        query: &crate::query::OrgListQuotaAttachmentsQuery,
    ) -> ItemStream<forgejo_model::QuotaUsedAttachment> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/quota/attachments", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the attachments affecting the organization's quota
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/quota/attachments` — needs the `read:organization` token scope.
    pub async fn list_quota_attachments_page(
        &self,
        org: &str,
        query: &crate::query::OrgListQuotaAttachmentsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::QuotaUsedAttachment>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/quota/attachments", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List the packages affecting the organization's quota
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/quota/packages` — needs the `read:organization` token scope.
    pub fn list_quota_packages(
        &self,
        org: &str,
        query: &crate::query::OrgListQuotaPackagesQuery,
    ) -> ItemStream<forgejo_model::QuotaUsedPackage> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/quota/packages", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the packages affecting the organization's quota
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/quota/packages` — needs the `read:organization` token scope.
    pub async fn list_quota_packages_page(
        &self,
        org: &str,
        query: &crate::query::OrgListQuotaPackagesQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::QuotaUsedPackage>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/quota/packages", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List an organization's repos
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/repos` — needs the `read:organization` token scope.
    pub fn list_repos(
        &self,
        org: &str,
        query: &crate::query::OrgListReposQuery,
    ) -> ItemStream<forgejo_model::Repository> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/repos", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List an organization's repos
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/repos` — needs the `read:organization` token scope.
    pub async fn list_repos_page(
        &self,
        org: &str,
        query: &crate::query::OrgListReposQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Repository>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/repos", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List a team's activity feeds
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /teams/{id}/activities/feeds` — needs the `read:organization` token scope.
    pub fn list_team_activity_feeds(
        &self,
        id: i64,
        query: &crate::query::OrgListTeamActivityFeedsQuery,
    ) -> ItemStream<forgejo_model::Activity> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/teams/{}/activities/feeds", id)).scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a team's activity feeds
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /teams/{id}/activities/feeds` — needs the `read:organization` token scope.
    pub async fn list_team_activity_feeds_page(
        &self,
        id: i64,
        query: &crate::query::OrgListTeamActivityFeedsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Activity>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/teams/{}/activities/feeds", id)).scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List a particular member of team
    ///
    /// `GET /teams/{id}/members/{username}` — needs the `read:organization` token scope.
    pub async fn list_team_member(&self, id: i64, username: &str) -> Result<forgejo_model::User> {
        let req = Request::get(format!("/teams/{}/members/{}", id, encode::seg(username)))
            .scope("read:organization");
        self.client.json(req).await
    }
    /// List a team's members
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /teams/{id}/members` — needs the `read:organization` token scope.
    pub fn list_team_members(
        &self,
        id: i64,
        query: &crate::query::OrgListTeamMembersQuery,
    ) -> ItemStream<forgejo_model::User> {
        let req = crate::stream_request(
            query.apply(Request::get(format!("/teams/{}/members", id)).scope("read:organization")),
            "page",
        );
        self.client.items(req)
    }
    /// List a team's members
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /teams/{id}/members` — needs the `read:organization` token scope.
    pub async fn list_team_members_page(
        &self,
        id: i64,
        query: &crate::query::OrgListTeamMembersQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get(format!("/teams/{}/members", id)).scope("read:organization")),
            paging,
        );
        self.client.page(req).await
    }
    /// List a particular repo of team
    ///
    /// `GET /teams/{id}/repos/{org}/{repo}` — needs the `read:organization` token scope.
    pub async fn list_team_repo(
        &self,
        id: i64,
        org: &str,
        repo: &str,
    ) -> Result<forgejo_model::Repository> {
        let req =
            Request::get(format!("/teams/{}/repos/{}/{}", id, encode::seg(org), encode::seg(repo)))
                .scope("read:organization");
        self.client.json(req).await
    }
    /// List a team's repos
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /teams/{id}/repos` — needs the `read:organization` token scope.
    pub fn list_team_repos(
        &self,
        id: i64,
        query: &crate::query::OrgListTeamReposQuery,
    ) -> ItemStream<forgejo_model::Repository> {
        let req = crate::stream_request(
            query.apply(Request::get(format!("/teams/{}/repos", id)).scope("read:organization")),
            "page",
        );
        self.client.items(req)
    }
    /// List a team's repos
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /teams/{id}/repos` — needs the `read:organization` token scope.
    pub async fn list_team_repos_page(
        &self,
        id: i64,
        query: &crate::query::OrgListTeamReposQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Repository>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get(format!("/teams/{}/repos", id)).scope("read:organization")),
            paging,
        );
        self.client.page(req).await
    }
    /// List an organization's teams
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/teams` — needs the `read:organization` token scope.
    pub fn list_teams(
        &self,
        org: &str,
        query: &crate::query::OrgListTeamsQuery,
    ) -> ItemStream<forgejo_model::Team> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/teams", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List an organization's teams
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/teams` — needs the `read:organization` token scope.
    pub async fn list_teams_page(
        &self,
        org: &str,
        query: &crate::query::OrgListTeamsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Team>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/teams", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List a user's organizations
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /users/{username}/orgs` — needs the `read:organization` token scope.
    pub fn list_user_orgs(
        &self,
        username: &str,
        query: &crate::query::OrgListUserOrgsQuery,
    ) -> ItemStream<forgejo_model::Organization> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/users/{}/orgs", encode::seg(username)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a user's organizations
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /users/{username}/orgs` — needs the `read:organization` token scope.
    pub async fn list_user_orgs_page(
        &self,
        username: &str,
        query: &crate::query::OrgListUserOrgsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Organization>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/users/{}/orgs", encode::seg(username)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
}
