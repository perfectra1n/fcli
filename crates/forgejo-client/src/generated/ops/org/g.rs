// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `org` operations: `g`.
use forgejo_core::Result;
use forgejo_core::http::{ItemStream, PageInfo, Paging, Request, encode};
impl super::Org<'_> {
    /// Get an organization
    ///
    /// `GET /orgs/{org}` — needs the `read:organization` token scope.
    pub async fn get(&self, org: &str) -> Result<forgejo_model::Organization> {
        let req = Request::get(format!("/orgs/{}", encode::seg(org))).scope("read:organization");
        self.client.json(req).await
    }
    /// List all organizations
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs` — needs the `read:organization` token scope.
    pub fn get_all(
        &self,
        query: &crate::query::OrgGetAllQuery,
    ) -> ItemStream<forgejo_model::Organization> {
        let req = crate::stream_request(
            query.apply(Request::get("/orgs").scope("read:organization")),
            "page",
        );
        self.client.items(req)
    }
    /// List all organizations
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs` — needs the `read:organization` token scope.
    pub async fn get_all_page(
        &self,
        query: &crate::query::OrgGetAllQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Organization>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/orgs").scope("read:organization")),
            paging,
        );
        self.client.page(req).await
    }
    /// Get a hook
    ///
    /// `GET /orgs/{org}/hooks/{id}` — needs the `read:organization` token scope.
    pub async fn get_hook(&self, org: &str, id: i64) -> Result<forgejo_model::Hook> {
        let req = Request::get(format!("/orgs/{}/hooks/{}", encode::seg(org), id))
            .scope("read:organization");
        self.client.json(req).await
    }
    /// Get a single label
    ///
    /// `GET /orgs/{org}/labels/{id}` — needs the `read:organization` token scope.
    pub async fn get_label(&self, org: &str, id: i64) -> Result<forgejo_model::Label> {
        let req = Request::get(format!("/orgs/{}/labels/{}", encode::seg(org), id))
            .scope("read:organization");
        self.client.json(req).await
    }
    /// Get a particular runner that belongs to the organization
    ///
    /// `GET /orgs/{org}/actions/runners/{runner_id}` — needs the `read:organization` token scope.
    pub async fn get_org_runner(
        &self,
        org: &str,
        runner_id: &str,
    ) -> Result<forgejo_model::ActionRunner> {
        let req = Request::get(format!(
            "/orgs/{}/actions/runners/{}",
            encode::seg(org),
            encode::seg(runner_id)
        ))
        .scope("read:organization");
        self.client.json(req).await
    }
    /// Get the organization's runners
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/actions/runners` — needs the `read:organization` token scope.
    pub fn get_org_runners(
        &self,
        org: &str,
        query: &crate::query::GetOrgRunnersQuery,
    ) -> ItemStream<forgejo_model::ActionRunner> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/actions/runners", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get the organization's runners
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/actions/runners` — needs the `read:organization` token scope.
    pub async fn get_org_runners_page(
        &self,
        org: &str,
        query: &crate::query::GetOrgRunnersQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::ActionRunner>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/actions/runners", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get organization's variable by name
    ///
    /// `GET /orgs/{org}/actions/variables/{variablename}` — needs the `read:organization` token scope.
    pub async fn get_org_variable(
        &self,
        org: &str,
        variablename: &str,
    ) -> Result<forgejo_model::ActionVariable> {
        let req = Request::get(format!(
            "/orgs/{}/actions/variables/{}",
            encode::seg(org),
            encode::seg(variablename)
        ))
        .scope("read:organization");
        self.client.json(req).await
    }
    /// List variables of an organization
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/actions/variables` — needs the `read:organization` token scope.
    pub fn get_org_variables_list(
        &self,
        org: &str,
        query: &crate::query::GetOrgVariablesListQuery,
    ) -> ItemStream<forgejo_model::ActionVariable> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/actions/variables", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List variables of an organization
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/actions/variables` — needs the `read:organization` token scope.
    pub async fn get_org_variables_list_page(
        &self,
        org: &str,
        query: &crate::query::GetOrgVariablesListQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::ActionVariable>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/actions/variables", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get quota information for an organization
    ///
    /// `GET /orgs/{org}/quota` — needs the `read:organization` token scope.
    pub async fn get_quota(&self, org: &str) -> Result<forgejo_model::QuotaInfo> {
        let req =
            Request::get(format!("/orgs/{}/quota", encode::seg(org))).scope("read:organization");
        self.client.json(req).await
    }
    /// Get the organization's runner registration token This operation has been deprecated in Forgejo
    /// 15. Use the web UI or \[`/orgs/{org}/actions/runners`\](#/organization/registerOrgRunner)
    /// instead.
    ///
    /// `GET /orgs/{org}/actions/runners/registration-token` — needs the `read:organization` token scope.
    #[deprecated(
        note = "deprecated by the Forgejo API; see the endpoint's documentation for the replacement"
    )]
    pub async fn get_runner_registration_token(
        &self,
        org: &str,
    ) -> Result<forgejo_model::RegistrationToken> {
        let req =
            Request::get(format!("/orgs/{}/actions/runners/registration-token", encode::seg(org)))
                .scope("read:organization");
        self.client.json(req).await
    }
    /// Get a team
    ///
    /// `GET /teams/{id}` — needs the `read:organization` token scope.
    pub async fn get_team(&self, id: i64) -> Result<forgejo_model::Team> {
        let req = Request::get(format!("/teams/{}", id)).scope("read:organization");
        self.client.json(req).await
    }
    /// Get user permissions in organization
    ///
    /// `GET /users/{username}/orgs/{org}/permissions` — needs the `read:organization` token scope.
    pub async fn get_user_permissions(
        &self,
        username: &str,
        org: &str,
    ) -> Result<forgejo_model::OrganizationPermissions> {
        let req = Request::get(format!(
            "/users/{}/orgs/{}/permissions",
            encode::seg(username),
            encode::seg(org)
        ))
        .scope("read:organization");
        self.client.json(req).await
    }
}
