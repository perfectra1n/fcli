// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `issue` operations: `l`.
use forgejo_core::Result;
use forgejo_core::http::{ItemStream, PageInfo, Paging, Request, encode};
impl super::Issue<'_> {
    /// List issues that are blocked by this issue
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/blocks` — needs the `read:issue` token scope.
    pub fn list_blocks(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::IssueListBlocksQuery,
    ) -> ItemStream<forgejo_model::Issue> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/issues/{}/blocks",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:issue"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List issues that are blocked by this issue
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/blocks` — needs the `read:issue` token scope.
    pub async fn list_blocks_page(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::IssueListBlocksQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Issue>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/issues/{}/blocks",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:issue"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List issue's attachments
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/assets` — needs the `read:issue` token scope.
    pub async fn list_issue_attachments(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
    ) -> Result<Vec<forgejo_model::Attachment>> {
        let req = Request::get(format!(
            "/repos/{}/{}/issues/{}/assets",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("read:issue");
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::Attachment>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// List comment's attachments
    ///
    /// `GET /repos/{owner}/{repo}/issues/comments/{id}/assets` — needs the `read:issue` token scope.
    pub async fn list_issue_comment_attachments(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
    ) -> Result<Vec<forgejo_model::Attachment>> {
        let req = Request::get(format!(
            "/repos/{}/{}/issues/comments/{}/assets",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("read:issue");
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::Attachment>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// List an issue's dependencies, i.e all issues that block this issue.
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/dependencies` — needs the `read:issue` token scope.
    pub fn list_issue_dependencies(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::IssueListIssueDependenciesQuery,
    ) -> ItemStream<forgejo_model::Issue> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/issues/{}/dependencies",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:issue"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List an issue's dependencies, i.e all issues that block this issue.
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/dependencies` — needs the `read:issue` token scope.
    pub async fn list_issue_dependencies_page(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::IssueListIssueDependenciesQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Issue>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/issues/{}/dependencies",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:issue"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List a repository's issues
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/issues` — needs the `read:issue` token scope.
    pub fn list_issues(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::IssueListIssuesQuery,
    ) -> ItemStream<forgejo_model::Issue> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/issues", encode::seg(owner), encode::seg(repo)))
                    .scope("read:issue"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a repository's issues
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/issues` — needs the `read:issue` token scope.
    pub async fn list_issues_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::IssueListIssuesQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Issue>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/issues", encode::seg(owner), encode::seg(repo)))
                    .scope("read:issue"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get all of a repository's labels
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/labels` — needs the `read:issue` token scope.
    pub fn list_labels(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::IssueListLabelsQuery,
    ) -> ItemStream<forgejo_model::Label> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/labels", encode::seg(owner), encode::seg(repo)))
                    .scope("read:issue"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get all of a repository's labels
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/labels` — needs the `read:issue` token scope.
    pub async fn list_labels_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::IssueListLabelsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Label>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/labels", encode::seg(owner), encode::seg(repo)))
                    .scope("read:issue"),
            ),
            paging,
        );
        self.client.page(req).await
    }
}
