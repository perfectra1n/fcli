// @generated by `cargo xtask codegen` from spec/forgejo-16.0.5.json
// (sha256:6d3563a56631a92b). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `issue` operations: `r`.
use forgejo_core::Result;
use forgejo_core::http::{Request, encode};
impl super::Issue<'_> {
    /// Unblock the issue given in the body by the issue in path
    ///
    /// `DELETE /repos/{owner}/{repo}/issues/{index}/blocks` — needs the `write:issue` token scope.
    pub async fn remove_issue_blocking(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &forgejo_model::IssueMeta,
    ) -> Result<forgejo_model::Issue> {
        let req = Request::delete(format!(
            "/repos/{}/{}/issues/{}/blocks",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Remove an issue dependency
    ///
    /// `DELETE /repos/{owner}/{repo}/issues/{index}/dependencies` — needs the `write:issue` token scope.
    pub async fn remove_issue_dependencies(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &forgejo_model::IssueMeta,
    ) -> Result<forgejo_model::Issue> {
        let req = Request::delete(format!(
            "/repos/{}/{}/issues/{}/dependencies",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Remove a label from an issue
    ///
    /// `DELETE /repos/{owner}/{repo}/issues/{index}/labels/{identifier}` — needs the `write:issue` token scope.
    pub async fn remove_label(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        identifier: &str,
        body: &forgejo_model::DeleteLabelsOption,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/issues/{}/labels/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            encode::seg(identifier)
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.empty(req).await
    }
    /// Replace an issue's labels
    ///
    /// `PUT /repos/{owner}/{repo}/issues/{index}/labels` — needs the `write:issue` token scope.
    pub async fn replace_labels(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &forgejo_model::IssueLabelsOption,
    ) -> Result<Vec<forgejo_model::Label>> {
        let req = Request::put(format!(
            "/repos/{}/{}/issues/{}/labels",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        Ok(self.client.json::<Option<Vec<forgejo_model::Label>>>(req).await?.unwrap_or_default())
    }
    /// Reset a tracked time of an issue
    ///
    /// `DELETE /repos/{owner}/{repo}/issues/{index}/times` — needs the `write:issue` token scope.
    pub async fn reset_time(&self, owner: &str, repo: &str, index: i64) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/issues/{}/times",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue");
        self.client.empty(req).await
    }
}
