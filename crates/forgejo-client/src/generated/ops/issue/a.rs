// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `issue` operations: `a`.
use forgejo_core::Result;
use forgejo_core::http::{Request, encode};
impl super::Issue<'_> {
    /// Add a label to an issue
    ///
    /// `POST /repos/{owner}/{repo}/issues/{index}/labels` — needs the `write:issue` token scope.
    pub async fn add_label(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &forgejo_model::IssueLabelsOption,
    ) -> Result<Vec<forgejo_model::Label>> {
        let req = Request::post(format!(
            "/repos/{}/{}/issues/{}/labels",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        Ok(self.client.json::<Option<Vec<forgejo_model::Label>>>(req).await?.unwrap_or_default())
    }
    /// Subscribe user to issue
    ///
    /// `PUT /repos/{owner}/{repo}/issues/{index}/subscriptions/{user}` — needs the `write:issue` token scope.
    pub async fn add_subscription(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        user: &str,
    ) -> Result<()> {
        let req = Request::put(format!(
            "/repos/{}/{}/issues/{}/subscriptions/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            encode::seg(user)
        ))
        .scope("write:issue");
        self.client.empty(req).await
    }
    /// Add tracked time to a issue
    ///
    /// `POST /repos/{owner}/{repo}/issues/{index}/times` — needs the `write:issue` token scope.
    pub async fn add_time(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &forgejo_model::AddTimeOption,
    ) -> Result<forgejo_model::TrackedTime> {
        let req = Request::post(format!(
            "/repos/{}/{}/issues/{}/times",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
}
