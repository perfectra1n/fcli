// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `issue` operations: `e`.
use forgejo_core::Result;
use forgejo_core::http::{Request, encode};
impl super::Issue<'_> {
    /// Edit a comment
    ///
    /// `PATCH /repos/{owner}/{repo}/issues/comments/{id}` — needs the `write:issue` token scope.
    pub async fn edit_comment(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        body: &forgejo_model::EditIssueCommentOption,
    ) -> Result<forgejo_model::Comment> {
        let req = Request::patch(format!(
            "/repos/{}/{}/issues/comments/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Edit a comment
    ///
    /// `PATCH /repos/{owner}/{repo}/issues/{index}/comments/{id}` — needs the `write:issue` token scope.
    #[deprecated(
        note = "deprecated by the Forgejo API; see the endpoint's documentation for the replacement"
    )]
    pub async fn edit_comment_deprecated(
        &self,
        owner: &str,
        repo: &str,
        index: i32,
        id: i64,
        body: &forgejo_model::EditIssueCommentOption,
    ) -> Result<forgejo_model::Comment> {
        let req = Request::patch(format!(
            "/repos/{}/{}/issues/{}/comments/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            id
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Edit an issue. If using deadline only the date will be taken into account, and time of day
    /// ignored.
    ///
    /// `PATCH /repos/{owner}/{repo}/issues/{index}` — needs the `write:issue` token scope.
    pub async fn edit_issue(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &forgejo_model::EditIssueOption,
    ) -> Result<forgejo_model::Issue> {
        let req = Request::patch(format!(
            "/repos/{}/{}/issues/{}",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Edit an issue attachment
    ///
    /// `PATCH /repos/{owner}/{repo}/issues/{index}/assets/{attachment_id}` — needs the `write:issue` token scope.
    pub async fn edit_issue_attachment(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        attachment_id: i64,
        body: &forgejo_model::EditAttachmentOptions,
    ) -> Result<forgejo_model::Attachment> {
        let req = Request::patch(format!(
            "/repos/{}/{}/issues/{}/assets/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            attachment_id
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Edit a comment attachment
    ///
    /// `PATCH /repos/{owner}/{repo}/issues/comments/{id}/assets/{attachment_id}` — needs the `write:issue` token scope.
    pub async fn edit_issue_comment_attachment(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        attachment_id: i64,
        body: &forgejo_model::EditAttachmentOptions,
    ) -> Result<forgejo_model::Attachment> {
        let req = Request::patch(format!(
            "/repos/{}/{}/issues/comments/{}/assets/{}",
            encode::seg(owner),
            encode::seg(repo),
            id,
            attachment_id
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Set an issue deadline. If set to null, the deadline is deleted. If using deadline only the date
    /// will be taken into account, and time of day ignored.
    ///
    /// `POST /repos/{owner}/{repo}/issues/{index}/deadline` — needs the `write:issue` token scope.
    pub async fn edit_issue_deadline(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &forgejo_model::EditDeadlineOption,
    ) -> Result<forgejo_model::IssueDeadline> {
        let req = Request::post(format!(
            "/repos/{}/{}/issues/{}/deadline",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Update a label
    ///
    /// `PATCH /repos/{owner}/{repo}/labels/{id}` — needs the `write:issue` token scope.
    pub async fn edit_label(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        body: &forgejo_model::EditLabelOption,
    ) -> Result<forgejo_model::Label> {
        let req = Request::patch(format!(
            "/repos/{}/{}/labels/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Update a milestone
    ///
    /// `PATCH /repos/{owner}/{repo}/milestones/{id}` — needs the `write:issue` token scope.
    pub async fn edit_milestone(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        body: &forgejo_model::EditMilestoneOption,
    ) -> Result<forgejo_model::Milestone> {
        let req = Request::patch(format!(
            "/repos/{}/{}/milestones/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
}
