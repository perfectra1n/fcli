// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `admin` — Instance administration: users, orgs, cron, runners, quotas.
use forgejo_core::Result;
use forgejo_core::http::{Client, ItemStream, PageInfo, Paging, Request, encode};
/// Instance administration: users, orgs, cron, runners, quotas.
///
/// Obtain one from `Api::admin`, or wrap a bare `Client` with `Admin::new`.
#[derive(Debug, Clone, Copy)]
pub struct Admin<'a> {
    pub(crate) client: &'a Client,
}
impl<'a> Admin<'a> {
    /// Borrow a client as this operation group. Free — no allocation, no clone.
    pub fn new(client: &'a Client) -> Self {
        Self { client }
    }
    /// Adds a rule to a quota group
    ///
    /// `PUT /admin/quota/groups/{quotagroup}/rules/{quotarule}` — needs the `write:admin` token scope.
    pub async fn add_rule_to_quota_group(&self, quotagroup: &str, quotarule: &str) -> Result<()> {
        let req = Request::put(format!(
            "/admin/quota/groups/{}/rules/{}",
            encode::seg(quotagroup),
            encode::seg(quotarule)
        ))
        .scope("write:admin");
        self.client.empty(req).await
    }
    /// Add a user to a quota group
    ///
    /// `PUT /admin/quota/groups/{quotagroup}/users/{username}` — needs the `write:admin` token scope.
    pub async fn add_user_to_quota_group(&self, quotagroup: &str, username: &str) -> Result<()> {
        let req = Request::put(format!(
            "/admin/quota/groups/{}/users/{}",
            encode::seg(quotagroup),
            encode::seg(username)
        ))
        .scope("write:admin");
        self.client.empty(req).await
    }
    /// Adopt unadopted files as a repository
    ///
    /// `POST /admin/unadopted/{owner}/{repo}` — needs the `write:admin` token scope.
    pub async fn adopt_repository(&self, owner: &str, repo: &str) -> Result<()> {
        let req =
            Request::post(format!("/admin/unadopted/{}/{}", encode::seg(owner), encode::seg(repo)))
                .scope("write:admin");
        self.client.empty(req).await
    }
    /// Create a hook
    ///
    /// `POST /admin/hooks` — needs the `write:admin` token scope.
    pub async fn create_hook(
        &self,
        body: &forgejo_model::CreateHookOption,
    ) -> Result<forgejo_model::Hook> {
        let req = Request::post("/admin/hooks").scope("write:admin").json_body(body)?;
        self.client.json(req).await
    }
    /// Create an organization
    ///
    /// `POST /admin/users/{username}/orgs` — needs the `write:admin` token scope.
    pub async fn create_org(
        &self,
        username: &str,
        body: &forgejo_model::CreateOrgOption,
    ) -> Result<forgejo_model::Organization> {
        let req = Request::post(format!("/admin/users/{}/orgs", encode::seg(username)))
            .scope("write:admin")
            .json_body(body)?;
        self.client.json(req).await
    }
    /// Add an SSH public key to user's account
    ///
    /// `POST /admin/users/{username}/keys` — needs the `write:admin` token scope.
    pub async fn create_public_key(
        &self,
        username: &str,
        body: &forgejo_model::CreateKeyOption,
    ) -> Result<forgejo_model::PublicKey> {
        let req = Request::post(format!("/admin/users/{}/keys", encode::seg(username)))
            .scope("write:admin")
            .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a new quota group
    ///
    /// `POST /admin/quota/groups` — needs the `write:admin` token scope.
    pub async fn create_quota_group(
        &self,
        body: &forgejo_model::CreateQuotaGroupOptions,
    ) -> Result<forgejo_model::QuotaGroup> {
        let req = Request::post("/admin/quota/groups").scope("write:admin").json_body(body)?;
        self.client.json(req).await
    }
    /// Create a new quota rule
    ///
    /// `POST /admin/quota/rules` — needs the `write:admin` token scope.
    pub async fn create_quota_rule(
        &self,
        body: &forgejo_model::CreateQuotaRuleOptions,
    ) -> Result<forgejo_model::QuotaRuleInfo> {
        let req = Request::post("/admin/quota/rules").scope("write:admin").json_body(body)?;
        self.client.json(req).await
    }
    /// Create a repository on behalf of a user
    ///
    /// `POST /admin/users/{username}/repos` — needs the `write:admin` token scope.
    pub async fn create_repo(
        &self,
        username: &str,
        body: &forgejo_model::CreateRepoOption,
    ) -> Result<forgejo_model::Repository> {
        let req = Request::post(format!("/admin/users/{}/repos", encode::seg(username)))
            .scope("write:admin")
            .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a user account
    ///
    /// `POST /admin/users` — needs the `write:admin` token scope.
    pub async fn create_user(
        &self,
        body: &forgejo_model::CreateUserOption,
    ) -> Result<forgejo_model::User> {
        let req = Request::post("/admin/users").scope("write:admin").json_body(body)?;
        self.client.json(req).await
    }
    /// Create an access token for the specified user
    ///
    /// `POST /admin/users/{username}/tokens` — needs the `write:admin` token scope.
    pub async fn create_user_access_token(
        &self,
        username: &str,
        body: &forgejo_model::CreateAccessTokenOption,
    ) -> Result<forgejo_model::AccessToken> {
        let req = Request::post(format!("/admin/users/{}/tokens", encode::seg(username)))
            .scope("write:admin")
            .json_body(body)?;
        self.client.json(req).await
    }
    /// List cron tasks
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /admin/cron` — needs the `read:admin` token scope.
    pub fn cron_list(
        &self,
        query: &crate::query::AdminCronListQuery,
    ) -> ItemStream<forgejo_model::Cron> {
        let req = crate::stream_request(
            query.apply(Request::get("/admin/cron").scope("read:admin")),
            "page",
        );
        self.client.items(req)
    }
    /// List cron tasks
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /admin/cron` — needs the `read:admin` token scope.
    pub async fn cron_list_page(
        &self,
        query: &crate::query::AdminCronListQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Cron>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/admin/cron").scope("read:admin")),
            paging,
        );
        self.client.page(req).await
    }
    /// Run cron task
    ///
    /// `POST /admin/cron/{task}` — needs the `write:admin` token scope.
    pub async fn cron_run(&self, task: &str) -> Result<()> {
        let req = Request::post(format!("/admin/cron/{}", encode::seg(task))).scope("write:admin");
        self.client.empty(req).await
    }
    /// Delete a particular runner, no matter whether it is a global runner or scoped to an
    /// organization, user, or repository
    ///
    /// `DELETE /admin/actions/runners/{runner_id}` — needs the `write:admin` token scope.
    pub async fn delete_admin_runner(&self, runner_id: &str) -> Result<()> {
        let req = Request::delete(format!("/admin/actions/runners/{}", encode::seg(runner_id)))
            .scope("write:admin");
        self.client.empty(req).await
    }
    /// Delete a hook
    ///
    /// `DELETE /admin/hooks/{id}` — needs the `write:admin` token scope.
    pub async fn delete_hook(&self, id: i64) -> Result<()> {
        let req = Request::delete(format!("/admin/hooks/{}", id)).scope("write:admin");
        self.client.empty(req).await
    }
    /// Delete a quota group
    ///
    /// `DELETE /admin/quota/groups/{quotagroup}` — needs the `write:admin` token scope.
    pub async fn delete_quota_group(&self, quotagroup: &str) -> Result<()> {
        let req = Request::delete(format!("/admin/quota/groups/{}", encode::seg(quotagroup)))
            .scope("write:admin");
        self.client.empty(req).await
    }
    /// Deletes a quota rule
    ///
    /// `DELETE /admin/quota/rules/{quotarule}` — needs the `write:admin` token scope.
    pub async fn delete_quota_rule(&self, quotarule: &str) -> Result<()> {
        let req = Request::delete(format!("/admin/quota/rules/{}", encode::seg(quotarule)))
            .scope("write:admin");
        self.client.empty(req).await
    }
    /// Delete unadopted files
    ///
    /// `DELETE /admin/unadopted/{owner}/{repo}` — needs the `write:admin` token scope.
    pub async fn delete_unadopted_repository(&self, owner: &str, repo: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/admin/unadopted/{}/{}",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:admin");
        self.client.empty(req).await
    }
    /// Delete user account
    ///
    /// `DELETE /admin/users/{username}` — needs the `write:admin` token scope.
    pub async fn delete_user(
        &self,
        username: &str,
        query: &crate::query::AdminDeleteUserQuery,
    ) -> Result<()> {
        let req = query.apply(
            Request::delete(format!("/admin/users/{}", encode::seg(username))).scope("write:admin"),
        );
        self.client.empty(req).await
    }
    /// Delete an access token for the specified user
    ///
    /// `DELETE /admin/users/{username}/tokens/{token}` — needs the `write:admin` token scope.
    pub async fn delete_user_access_token(&self, username: &str, token: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/admin/users/{}/tokens/{}",
            encode::seg(username),
            encode::seg(token)
        ))
        .scope("write:admin");
        self.client.empty(req).await
    }
    /// Delete email addresses from a user's account
    ///
    /// `DELETE /admin/users/{username}/emails` — needs the `write:admin` token scope.
    pub async fn delete_user_emails(
        &self,
        username: &str,
        body: &forgejo_model::DeleteEmailOption,
    ) -> Result<()> {
        let req = Request::delete(format!("/admin/users/{}/emails", encode::seg(username)))
            .scope("write:admin")
            .json_body(body)?;
        self.client.empty(req).await
    }
    /// Remove a public key from user's account
    ///
    /// `DELETE /admin/users/{username}/keys/{id}` — needs the `write:admin` token scope.
    pub async fn delete_user_public_key(&self, username: &str, id: i64) -> Result<()> {
        let req = Request::delete(format!("/admin/users/{}/keys/{}", encode::seg(username), id))
            .scope("write:admin");
        self.client.empty(req).await
    }
    /// Update a hook
    ///
    /// `PATCH /admin/hooks/{id}` — needs the `write:admin` token scope.
    pub async fn edit_hook(
        &self,
        id: i64,
        body: &forgejo_model::EditHookOption,
    ) -> Result<forgejo_model::Hook> {
        let req =
            Request::patch(format!("/admin/hooks/{}", id)).scope("write:admin").json_body(body)?;
        self.client.json(req).await
    }
    /// Change an existing quota rule
    ///
    /// `PATCH /admin/quota/rules/{quotarule}` — needs the `write:admin` token scope.
    pub async fn edit_quota_rule(
        &self,
        quotarule: &str,
        body: &forgejo_model::EditQuotaRuleOptions,
    ) -> Result<forgejo_model::QuotaRuleInfo> {
        let req = Request::patch(format!("/admin/quota/rules/{}", encode::seg(quotarule)))
            .scope("write:admin")
            .json_body(body)?;
        self.client.json(req).await
    }
    /// Edit an existing user
    ///
    /// `PATCH /admin/users/{username}` — needs the `write:admin` token scope.
    pub async fn edit_user(
        &self,
        username: &str,
        body: &forgejo_model::EditUserOption,
    ) -> Result<forgejo_model::User> {
        let req = Request::patch(format!("/admin/users/{}", encode::seg(username)))
            .scope("write:admin")
            .json_body(body)?;
        self.client.json(req).await
    }
    /// Get action run jobs
    ///
    /// `GET /admin/actions/runners/jobs` — needs the `read:admin` token scope.
    pub async fn get_action_run_jobs(
        &self,
        query: &crate::query::AdminGetActionRunJobsQuery,
    ) -> Result<Vec<forgejo_model::ActionRunJob>> {
        let req = query.apply(Request::get("/admin/actions/runners/jobs").scope("read:admin"));
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::ActionRunJob>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Get a particular runner, no matter whether it is a global runner or scoped to an organization,
    /// user, or repository
    ///
    /// `GET /admin/actions/runners/{runner_id}` — needs the `read:admin` token scope.
    pub async fn get_admin_runner(&self, runner_id: &str) -> Result<forgejo_model::ActionRunner> {
        let req = Request::get(format!("/admin/actions/runners/{}", encode::seg(runner_id)))
            .scope("read:admin");
        self.client.json(req).await
    }
    /// Get all runners, no matter whether they are global runners or scoped to an organization, user,
    /// or repository
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /admin/actions/runners` — needs the `read:admin` token scope.
    pub fn get_admin_runners(
        &self,
        query: &crate::query::GetAdminRunnersQuery,
    ) -> ItemStream<forgejo_model::ActionRunner> {
        let req = crate::stream_request(
            query.apply(Request::get("/admin/actions/runners").scope("read:admin")),
            "page",
        );
        self.client.items(req)
    }
    /// Get all runners, no matter whether they are global runners or scoped to an organization, user,
    /// or repository
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /admin/actions/runners` — needs the `read:admin` token scope.
    pub async fn get_admin_runners_page(
        &self,
        query: &crate::query::GetAdminRunnersQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::ActionRunner>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/admin/actions/runners").scope("read:admin")),
            paging,
        );
        self.client.page(req).await
    }
    /// List all users' email addresses
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /admin/emails` — needs the `read:admin` token scope.
    pub fn get_all_emails(
        &self,
        query: &crate::query::AdminGetAllEmailsQuery,
    ) -> ItemStream<forgejo_model::Email> {
        let req = crate::stream_request(
            query.apply(Request::get("/admin/emails").scope("read:admin")),
            "page",
        );
        self.client.items(req)
    }
    /// List all users' email addresses
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /admin/emails` — needs the `read:admin` token scope.
    pub async fn get_all_emails_page(
        &self,
        query: &crate::query::AdminGetAllEmailsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Email>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/admin/emails").scope("read:admin")),
            paging,
        );
        self.client.page(req).await
    }
    /// List all organizations
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /admin/orgs` — needs the `read:admin` token scope.
    pub fn get_all_orgs(
        &self,
        query: &crate::query::AdminGetAllOrgsQuery,
    ) -> ItemStream<forgejo_model::Organization> {
        let req = crate::stream_request(
            query.apply(Request::get("/admin/orgs").scope("read:admin")),
            "page",
        );
        self.client.items(req)
    }
    /// List all organizations
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /admin/orgs` — needs the `read:admin` token scope.
    pub async fn get_all_orgs_page(
        &self,
        query: &crate::query::AdminGetAllOrgsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Organization>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/admin/orgs").scope("read:admin")),
            paging,
        );
        self.client.page(req).await
    }
    /// Get a hook
    ///
    /// `GET /admin/hooks/{id}` — needs the `read:admin` token scope.
    pub async fn get_hook(&self, id: i64) -> Result<forgejo_model::Hook> {
        let req = Request::get(format!("/admin/hooks/{}", id)).scope("read:admin");
        self.client.json(req).await
    }
    /// Get information about the quota group
    ///
    /// `GET /admin/quota/groups/{quotagroup}` — needs the `read:admin` token scope.
    pub async fn get_quota_group(&self, quotagroup: &str) -> Result<forgejo_model::QuotaGroup> {
        let req = Request::get(format!("/admin/quota/groups/{}", encode::seg(quotagroup)))
            .scope("read:admin");
        self.client.json(req).await
    }
    /// Get information about a quota rule
    ///
    /// `GET /admin/quota/rules/{quotarule}` — needs the `read:admin` token scope.
    pub async fn get_quota_rule(&self, quotarule: &str) -> Result<forgejo_model::QuotaRuleInfo> {
        let req = Request::get(format!("/admin/quota/rules/{}", encode::seg(quotarule)))
            .scope("read:admin");
        self.client.json(req).await
    }
    /// Get a runner registration token for registering global runners This operation has been
    /// deprecated in Forgejo 15. Use the web UI or
    /// \[`/admin/actions/runners`\](#/admin/registerAdminRunner) instead.
    ///
    /// `GET /admin/runners/registration-token` — needs the `read:admin` token scope.
    #[deprecated(
        note = "deprecated by the Forgejo API; see the endpoint's documentation for the replacement"
    )]
    pub async fn get_registration_token(&self) -> Result<forgejo_model::RegistrationToken> {
        let req = Request::get("/admin/runners/registration-token").scope("read:admin");
        self.client.json(req).await
    }
    /// Get a runner registration token for registering global runners This operation has been
    /// deprecated in Forgejo 15. Use the web UI or
    /// \[`/admin/actions/runners`\](#/admin/registerAdminRunner) instead.
    ///
    /// `GET /admin/actions/runners/registration-token` — needs the `read:admin` token scope.
    #[deprecated(
        note = "deprecated by the Forgejo API; see the endpoint's documentation for the replacement"
    )]
    pub async fn get_runner_registration_token(&self) -> Result<forgejo_model::RegistrationToken> {
        let req = Request::get("/admin/actions/runners/registration-token").scope("read:admin");
        self.client.json(req).await
    }
    /// Get the user's quota info
    ///
    /// `GET /admin/users/{username}/quota` — needs the `read:admin` token scope.
    pub async fn get_user_quota(&self, username: &str) -> Result<forgejo_model::QuotaInfo> {
        let req = Request::get(format!("/admin/users/{}/quota", encode::seg(username)))
            .scope("read:admin");
        self.client.json(req).await
    }
    /// List global (system) webhooks
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /admin/hooks` — needs the `read:admin` token scope.
    pub fn list_hooks(
        &self,
        query: &crate::query::AdminListHooksQuery,
    ) -> ItemStream<forgejo_model::Hook> {
        let req = crate::stream_request(
            query.apply(Request::get("/admin/hooks").scope("read:admin")),
            "page",
        );
        self.client.items(req)
    }
    /// List global (system) webhooks
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /admin/hooks` — needs the `read:admin` token scope.
    pub async fn list_hooks_page(
        &self,
        query: &crate::query::AdminListHooksQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Hook>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/admin/hooks").scope("read:admin")),
            paging,
        );
        self.client.page(req).await
    }
    /// List the available quota groups
    ///
    /// `GET /admin/quota/groups` — needs the `read:admin` token scope.
    pub async fn list_quota_groups(&self) -> Result<Vec<forgejo_model::QuotaGroup>> {
        let req = Request::get("/admin/quota/groups").scope("read:admin");
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::QuotaGroup>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// List the available quota rules
    ///
    /// `GET /admin/quota/rules` — needs the `read:admin` token scope.
    pub async fn list_quota_rules(&self) -> Result<Vec<forgejo_model::QuotaRuleInfo>> {
        let req = Request::get("/admin/quota/rules").scope("read:admin");
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::QuotaRuleInfo>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// List the specified user's access tokens
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /admin/users/{username}/tokens` — needs the `read:admin` token scope.
    pub fn list_user_access_tokens(
        &self,
        username: &str,
        query: &crate::query::AdminListUserAccessTokensQuery,
    ) -> ItemStream<forgejo_model::AccessToken> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/admin/users/{}/tokens", encode::seg(username)))
                    .scope("read:admin"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the specified user's access tokens
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /admin/users/{username}/tokens` — needs the `read:admin` token scope.
    pub async fn list_user_access_tokens_page(
        &self,
        username: &str,
        query: &crate::query::AdminListUserAccessTokensQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::AccessToken>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/admin/users/{}/tokens", encode::seg(username)))
                    .scope("read:admin"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List all email addresses for a user
    ///
    /// `GET /admin/users/{username}/emails` — needs the `read:admin` token scope.
    pub async fn list_user_emails(&self, username: &str) -> Result<Vec<forgejo_model::Email>> {
        let req = Request::get(format!("/admin/users/{}/emails", encode::seg(username)))
            .scope("read:admin");
        Ok(self.client.json::<Option<Vec<forgejo_model::Email>>>(req).await?.unwrap_or_default())
    }
    /// List users in a quota group
    ///
    /// `GET /admin/quota/groups/{quotagroup}/users` — needs the `read:admin` token scope.
    pub async fn list_users_in_quota_group(
        &self,
        quotagroup: &str,
    ) -> Result<Vec<forgejo_model::User>> {
        let req = Request::get(format!("/admin/quota/groups/{}/users", encode::seg(quotagroup)))
            .scope("read:admin");
        Ok(self.client.json::<Option<Vec<forgejo_model::User>>>(req).await?.unwrap_or_default())
    }
    /// Register a new global runner
    ///
    /// `POST /admin/actions/runners` — needs the `write:admin` token scope.
    pub async fn register_admin_runner(
        &self,
        body: &forgejo_model::RegisterRunnerOptions,
    ) -> Result<forgejo_model::RegisterRunnerResponse> {
        let req = Request::post("/admin/actions/runners").scope("write:admin").json_body(body)?;
        self.client.json(req).await
    }
    /// Removes a rule from a quota group
    ///
    /// `DELETE /admin/quota/groups/{quotagroup}/rules/{quotarule}` — needs the `write:admin` token scope.
    pub async fn remove_rule_from_quota_group(
        &self,
        quotagroup: &str,
        quotarule: &str,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/admin/quota/groups/{}/rules/{}",
            encode::seg(quotagroup),
            encode::seg(quotarule)
        ))
        .scope("write:admin");
        self.client.empty(req).await
    }
    /// Remove a user from a quota group
    ///
    /// `DELETE /admin/quota/groups/{quotagroup}/users/{username}` — needs the `write:admin` token scope.
    pub async fn remove_user_from_quota_group(
        &self,
        quotagroup: &str,
        username: &str,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/admin/quota/groups/{}/users/{}",
            encode::seg(quotagroup),
            encode::seg(username)
        ))
        .scope("write:admin");
        self.client.empty(req).await
    }
    /// Rename a user
    ///
    /// `POST /admin/users/{username}/rename` — needs the `write:admin` token scope.
    pub async fn rename_user(
        &self,
        username: &str,
        body: &forgejo_model::RenameUserOption,
    ) -> Result<()> {
        let req = Request::post(format!("/admin/users/{}/rename", encode::seg(username)))
            .scope("write:admin")
            .json_body(body)?;
        self.client.empty(req).await
    }
    /// Search users' email addresses
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /admin/emails/search` — needs the `read:admin` token scope.
    pub fn search_emails(
        &self,
        query: &crate::query::AdminSearchEmailsQuery,
    ) -> ItemStream<forgejo_model::Email> {
        let req = crate::stream_request(
            query.apply(Request::get("/admin/emails/search").scope("read:admin")),
            "page",
        );
        self.client.items(req)
    }
    /// Search users' email addresses
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /admin/emails/search` — needs the `read:admin` token scope.
    pub async fn search_emails_page(
        &self,
        query: &crate::query::AdminSearchEmailsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Email>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/admin/emails/search").scope("read:admin")),
            paging,
        );
        self.client.page(req).await
    }
    /// Search action jobs according to filter conditions This operation has been deprecated in Forgejo
    /// 15. Use \[`/admin/actions/runners/jobs`\](#/admin/adminGetActionRunJobs) instead.
    ///
    /// `GET /admin/runners/jobs` — needs the `read:admin` token scope.
    #[deprecated(
        note = "deprecated by the Forgejo API; see the endpoint's documentation for the replacement"
    )]
    pub async fn search_run_jobs(
        &self,
        query: &crate::query::AdminSearchRunJobsQuery,
    ) -> Result<Vec<forgejo_model::ActionRunJob>> {
        let req = query.apply(Request::get("/admin/runners/jobs").scope("read:admin"));
        Ok(self
            .client
            .json::<Option<Vec<forgejo_model::ActionRunJob>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Search users according filter conditions
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /admin/users` — needs the `read:admin` token scope.
    pub fn search_users(
        &self,
        query: &crate::query::AdminSearchUsersQuery,
    ) -> ItemStream<forgejo_model::User> {
        let req = crate::stream_request(
            query.apply(Request::get("/admin/users").scope("read:admin")),
            "page",
        );
        self.client.items(req)
    }
    /// Search users according filter conditions
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /admin/users` — needs the `read:admin` token scope.
    pub async fn search_users_page(
        &self,
        query: &crate::query::AdminSearchUsersQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/admin/users").scope("read:admin")),
            paging,
        );
        self.client.page(req).await
    }
    /// Set the user's quota groups to a given list.
    ///
    /// `POST /admin/users/{username}/quota/groups` — needs the `write:admin` token scope.
    pub async fn set_user_quota_groups(
        &self,
        username: &str,
        body: &forgejo_model::SetUserQuotaGroupsOptions,
    ) -> Result<()> {
        let req = Request::post(format!("/admin/users/{}/quota/groups", encode::seg(username)))
            .scope("write:admin")
            .json_body(body)?;
        self.client.empty(req).await
    }
    /// List unadopted repositories
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /admin/unadopted` — needs the `read:admin` token scope.
    pub fn unadopted_list(
        &self,
        query: &crate::query::AdminUnadoptedListQuery,
    ) -> ItemStream<String> {
        let req = crate::stream_request(
            query.apply(Request::get("/admin/unadopted").scope("read:admin")),
            "page",
        );
        self.client.items(req)
    }
    /// List unadopted repositories
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /admin/unadopted` — needs the `read:admin` token scope.
    pub async fn unadopted_list_page(
        &self,
        query: &crate::query::AdminUnadoptedListQuery,
        paging: Paging,
    ) -> Result<(Vec<String>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/admin/unadopted").scope("read:admin")),
            paging,
        );
        self.client.page(req).await
    }
}
