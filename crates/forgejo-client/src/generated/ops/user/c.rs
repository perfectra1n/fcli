// @generated by `cargo xtask codegen` from spec/forgejo-16.0.5.json
// (sha256:6d3563a56631a92b). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `user` operations: `c`.
use forgejo_core::Result;
use forgejo_core::http::{ItemStream, PageInfo, Paging, Request, encode};
impl super::User<'_> {
    /// Check if one user is following another user
    ///
    /// `GET /users/{username}/following/{target}` — needs the `read:user` token scope.
    pub async fn check_following(&self, username: &str, target: &str) -> Result<()> {
        let req = Request::get(format!(
            "/users/{}/following/{}",
            encode::seg(username),
            encode::seg(target)
        ))
        .scope("read:user");
        self.client.empty(req).await
    }
    /// Check if the authenticated user is over quota for a given subject
    ///
    /// `GET /user/quota/check` — needs the `read:user` token scope.
    pub async fn check_quota(&self, query: &crate::query::UserCheckQuotaQuery) -> Result<bool> {
        let req = query.apply(Request::get("/user/quota/check").scope("read:user"));
        self.client.json(req).await
    }
    /// Create a hook
    ///
    /// `POST /user/hooks` — needs the `write:user` token scope.
    pub async fn create_hook(
        &self,
        body: &forgejo_model::CreateHookOption,
    ) -> Result<forgejo_model::Hook> {
        let req = Request::post("/user/hooks").scope("write:user").json_body(body)?;
        self.client.json(req).await
    }
    /// Creates a new OAuth2 application
    ///
    /// `POST /user/applications/oauth2` — needs the `write:user` token scope.
    pub async fn create_oauth2_application(
        &self,
        body: &forgejo_model::CreateOAuth2ApplicationOptions,
    ) -> Result<forgejo_model::OAuth2Application> {
        let req = Request::post("/user/applications/oauth2").scope("write:user").json_body(body)?;
        self.client.json(req).await
    }
    /// Generate an access token for the specified user
    ///
    /// `POST /users/{username}/tokens` — needs the `write:user` token scope.
    pub async fn create_token(
        &self,
        username: &str,
        body: &forgejo_model::CreateAccessTokenOption,
    ) -> Result<forgejo_model::AccessToken> {
        let req = Request::post(format!("/users/{}/tokens", encode::seg(username)))
            .scope("write:user")
            .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a user-level variable
    ///
    /// `POST /user/actions/variables/{variablename}` — needs the `write:user` token scope.
    pub async fn create_user_variable(
        &self,
        variablename: &str,
        body: &forgejo_model::CreateVariableOption,
    ) -> Result<()> {
        let req = Request::post(format!("/user/actions/variables/{}", encode::seg(variablename)))
            .scope("write:user")
            .json_body(body)?;
        self.client.empty(req).await
    }
    /// Follow a remote activitypub account
    ///
    /// `POST /user/activitypub/follow` — needs the `write:user` token scope.
    pub async fn current_activity_pub_follow(
        &self,
        body: &forgejo_model::ApRemoteFollowOption,
    ) -> Result<()> {
        let req = Request::post("/user/activitypub/follow").scope("write:user").json_body(body)?;
        self.client.empty(req).await
    }
    /// Check whether a user is followed by the authenticated user
    ///
    /// `GET /user/following/{username}` — needs the `read:user` token scope.
    pub async fn current_check_following(&self, username: &str) -> Result<()> {
        let req =
            Request::get(format!("/user/following/{}", encode::seg(username))).scope("read:user");
        self.client.empty(req).await
    }
    /// Whether the authenticated is starring the repo
    ///
    /// `GET /user/starred/{owner}/{repo}` — needs the `read:user` token scope.
    pub async fn current_check_starring(&self, owner: &str, repo: &str) -> Result<()> {
        let req =
            Request::get(format!("/user/starred/{}/{}", encode::seg(owner), encode::seg(repo)))
                .scope("read:user");
        self.client.empty(req).await
    }
    /// Check if the current user is watching a repo
    ///
    /// `GET /repos/{owner}/{repo}/subscription` — needs the `read:repository` token scope.
    pub async fn current_check_subscription(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<forgejo_model::WatchInfo> {
        let req = Request::get(format!(
            "/repos/{}/{}/subscription",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Unfollow a user
    ///
    /// `DELETE /user/following/{username}` — needs the `write:user` token scope.
    pub async fn current_delete_follow(&self, username: &str) -> Result<()> {
        let req = Request::delete(format!("/user/following/{}", encode::seg(username)))
            .scope("write:user");
        self.client.empty(req).await
    }
    /// Remove a GPG public key from current user's account
    ///
    /// `DELETE /user/gpg_keys/{id}` — needs the `write:user` token scope.
    pub async fn current_delete_gpg_key(&self, id: i64) -> Result<()> {
        let req = Request::delete(format!("/user/gpg_keys/{}", id)).scope("write:user");
        self.client.empty(req).await
    }
    /// Delete a public key
    ///
    /// `DELETE /user/keys/{id}` — needs the `write:user` token scope.
    pub async fn current_delete_key(&self, id: i64) -> Result<()> {
        let req = Request::delete(format!("/user/keys/{}", id)).scope("write:user");
        self.client.empty(req).await
    }
    /// Unstar the given repo
    ///
    /// `DELETE /user/starred/{owner}/{repo}` — needs the `write:user` token scope.
    pub async fn current_delete_star(&self, owner: &str, repo: &str) -> Result<()> {
        let req =
            Request::delete(format!("/user/starred/{}/{}", encode::seg(owner), encode::seg(repo)))
                .scope("write:user");
        self.client.empty(req).await
    }
    /// Unwatch a repo
    ///
    /// `DELETE /repos/{owner}/{repo}/subscription` — needs the `write:repository` token scope.
    pub async fn current_delete_subscription(&self, owner: &str, repo: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/subscription",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Get a GPG key
    ///
    /// `GET /user/gpg_keys/{id}` — needs the `read:user` token scope.
    pub async fn current_get_gpg_key(&self, id: i64) -> Result<forgejo_model::GpgKey> {
        let req = Request::get(format!("/user/gpg_keys/{}", id)).scope("read:user");
        self.client.json(req).await
    }
    /// Get a public key
    ///
    /// `GET /user/keys/{id}` — needs the `read:user` token scope.
    pub async fn current_get_key(&self, id: i64) -> Result<forgejo_model::PublicKey> {
        let req = Request::get(format!("/user/keys/{}", id)).scope("read:user");
        self.client.json(req).await
    }
    /// List the authenticated user's followers
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/followers` — needs the `read:user` token scope.
    pub fn current_list_followers(
        &self,
        query: &crate::query::UserCurrentListFollowersQuery,
    ) -> ItemStream<forgejo_model::User> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/followers").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List the authenticated user's followers
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/followers` — needs the `read:user` token scope.
    pub async fn current_list_followers_page(
        &self,
        query: &crate::query::UserCurrentListFollowersQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/followers").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// List the users that the authenticated user is following
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/following` — needs the `read:user` token scope.
    pub fn current_list_following(
        &self,
        query: &crate::query::UserCurrentListFollowingQuery,
    ) -> ItemStream<forgejo_model::User> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/following").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List the users that the authenticated user is following
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/following` — needs the `read:user` token scope.
    pub async fn current_list_following_page(
        &self,
        query: &crate::query::UserCurrentListFollowingQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/following").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// List the authenticated user's GPG keys
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/gpg_keys` — needs the `read:user` token scope.
    pub fn current_list_gpg_keys(
        &self,
        query: &crate::query::UserCurrentListGpgKeysQuery,
    ) -> ItemStream<forgejo_model::GpgKey> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/gpg_keys").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List the authenticated user's GPG keys
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/gpg_keys` — needs the `read:user` token scope.
    pub async fn current_list_gpg_keys_page(
        &self,
        query: &crate::query::UserCurrentListGpgKeysQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::GpgKey>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/gpg_keys").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// List the authenticated user's public keys
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/keys` — needs the `read:user` token scope.
    pub fn current_list_keys(
        &self,
        query: &crate::query::UserCurrentListKeysQuery,
    ) -> ItemStream<forgejo_model::PublicKey> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/keys").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List the authenticated user's public keys
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/keys` — needs the `read:user` token scope.
    pub async fn current_list_keys_page(
        &self,
        query: &crate::query::UserCurrentListKeysQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::PublicKey>, PageInfo)> {
        let req =
            crate::page_request(query.apply(Request::get("/user/keys").scope("read:user")), paging);
        self.client.page(req).await
    }
    /// List the repos that the authenticated user owns
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/repos` — needs the `read:user` token scope.
    pub fn current_list_repos(
        &self,
        query: &crate::query::UserCurrentListReposQuery,
    ) -> ItemStream<forgejo_model::Repository> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/repos").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List the repos that the authenticated user owns
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/repos` — needs the `read:user` token scope.
    pub async fn current_list_repos_page(
        &self,
        query: &crate::query::UserCurrentListReposQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Repository>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/repos").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// The repos that the authenticated user has starred
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/starred` — needs the `read:user` token scope.
    pub fn current_list_starred(
        &self,
        query: &crate::query::UserCurrentListStarredQuery,
    ) -> ItemStream<forgejo_model::Repository> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/starred").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// The repos that the authenticated user has starred
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/starred` — needs the `read:user` token scope.
    pub async fn current_list_starred_page(
        &self,
        query: &crate::query::UserCurrentListStarredQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Repository>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/starred").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// List repositories watched by the authenticated user
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/subscriptions` — needs the `read:user` token scope.
    pub fn current_list_subscriptions(
        &self,
        query: &crate::query::UserCurrentListSubscriptionsQuery,
    ) -> ItemStream<forgejo_model::Repository> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/subscriptions").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List repositories watched by the authenticated user
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/subscriptions` — needs the `read:user` token scope.
    pub async fn current_list_subscriptions_page(
        &self,
        query: &crate::query::UserCurrentListSubscriptionsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Repository>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/subscriptions").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// Add a GPG public key to current user's account
    ///
    /// `POST /user/gpg_keys` — needs the `write:user` token scope.
    pub async fn current_post_gpg_key(
        &self,
        body: &forgejo_model::CreateGpgKeyOption,
    ) -> Result<forgejo_model::GpgKey> {
        let req = Request::post("/user/gpg_keys").scope("write:user").json_body(body)?;
        self.client.json(req).await
    }
    /// Create a public key
    ///
    /// `POST /user/keys` — needs the `write:user` token scope.
    pub async fn current_post_key(
        &self,
        body: &forgejo_model::CreateKeyOption,
    ) -> Result<forgejo_model::PublicKey> {
        let req = Request::post("/user/keys").scope("write:user").json_body(body)?;
        self.client.json(req).await
    }
    /// Follow a user
    ///
    /// `PUT /user/following/{username}` — needs the `write:user` token scope.
    pub async fn current_put_follow(&self, username: &str) -> Result<()> {
        let req =
            Request::put(format!("/user/following/{}", encode::seg(username))).scope("write:user");
        self.client.empty(req).await
    }
    /// Star the given repo
    ///
    /// `PUT /user/starred/{owner}/{repo}` — needs the `write:user` token scope.
    pub async fn current_put_star(&self, owner: &str, repo: &str) -> Result<()> {
        let req =
            Request::put(format!("/user/starred/{}/{}", encode::seg(owner), encode::seg(repo)))
                .scope("write:user");
        self.client.empty(req).await
    }
    /// Watch a repo
    ///
    /// `PUT /repos/{owner}/{repo}/subscription` — needs the `write:repository` token scope.
    pub async fn current_put_subscription(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<forgejo_model::WatchInfo> {
        let req = Request::put(format!(
            "/repos/{}/{}/subscription",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:repository");
        self.client.json(req).await
    }
    /// List the current user's tracked times
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/times` — needs the `read:user` token scope.
    pub fn current_tracked_times(
        &self,
        query: &crate::query::UserCurrentTrackedTimesQuery,
    ) -> ItemStream<forgejo_model::TrackedTime> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/times").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List the current user's tracked times
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/times` — needs the `read:user` token scope.
    pub async fn current_tracked_times_page(
        &self,
        query: &crate::query::UserCurrentTrackedTimesQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::TrackedTime>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/times").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
}
