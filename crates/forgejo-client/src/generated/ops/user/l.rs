// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `user` operations: `l`.
use forgejo_core::Result;
use forgejo_core::http::{ItemStream, PageInfo, Paging, Request, encode};
impl super::User<'_> {
    /// List a user's activity feeds
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /users/{username}/activities/feeds` — needs the `read:user` token scope.
    pub fn list_activity_feeds(
        &self,
        username: &str,
        query: &crate::query::UserListActivityFeedsQuery,
    ) -> ItemStream<forgejo_model::Activity> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/users/{}/activities/feeds", encode::seg(username)))
                    .scope("read:user"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a user's activity feeds
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /users/{username}/activities/feeds` — needs the `read:user` token scope.
    pub async fn list_activity_feeds_page(
        &self,
        username: &str,
        query: &crate::query::UserListActivityFeedsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Activity>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/users/{}/activities/feeds", encode::seg(username)))
                    .scope("read:user"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List the authenticated user's blocked users
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/list_blocked` — needs the `read:user` token scope.
    pub fn list_blocked_users(
        &self,
        query: &crate::query::UserListBlockedUsersQuery,
    ) -> ItemStream<forgejo_model::BlockedUser> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/list_blocked").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List the authenticated user's blocked users
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/list_blocked` — needs the `read:user` token scope.
    pub async fn list_blocked_users_page(
        &self,
        query: &crate::query::UserListBlockedUsersQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::BlockedUser>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/list_blocked").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// List all email addresses of the current user
    ///
    /// `GET /user/emails` — needs the `read:user` token scope.
    pub async fn list_emails(&self) -> Result<Vec<forgejo_model::Email>> {
        let req = Request::get("/user/emails").scope("read:user");
        Ok(self.client.json::<Option<Vec<forgejo_model::Email>>>(req).await?.unwrap_or_default())
    }
    /// List the given user's followers
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /users/{username}/followers` — needs the `read:user` token scope.
    pub fn list_followers(
        &self,
        username: &str,
        query: &crate::query::UserListFollowersQuery,
    ) -> ItemStream<forgejo_model::User> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/users/{}/followers", encode::seg(username)))
                    .scope("read:user"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the given user's followers
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /users/{username}/followers` — needs the `read:user` token scope.
    pub async fn list_followers_page(
        &self,
        username: &str,
        query: &crate::query::UserListFollowersQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/users/{}/followers", encode::seg(username)))
                    .scope("read:user"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List the users that the given user is following
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /users/{username}/following` — needs the `read:user` token scope.
    pub fn list_following(
        &self,
        username: &str,
        query: &crate::query::UserListFollowingQuery,
    ) -> ItemStream<forgejo_model::User> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/users/{}/following", encode::seg(username)))
                    .scope("read:user"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the users that the given user is following
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /users/{username}/following` — needs the `read:user` token scope.
    pub async fn list_following_page(
        &self,
        username: &str,
        query: &crate::query::UserListFollowingQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/users/{}/following", encode::seg(username)))
                    .scope("read:user"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List the given user's GPG keys
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /users/{username}/gpg_keys` — needs the `read:user` token scope.
    pub fn list_gpg_keys(
        &self,
        username: &str,
        query: &crate::query::UserListGpgKeysQuery,
    ) -> ItemStream<forgejo_model::GpgKey> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/users/{}/gpg_keys", encode::seg(username)))
                    .scope("read:user"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the given user's GPG keys
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /users/{username}/gpg_keys` — needs the `read:user` token scope.
    pub async fn list_gpg_keys_page(
        &self,
        username: &str,
        query: &crate::query::UserListGpgKeysQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::GpgKey>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/users/{}/gpg_keys", encode::seg(username)))
                    .scope("read:user"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List the authenticated user's webhooks
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/hooks` — needs the `read:user` token scope.
    pub fn list_hooks(
        &self,
        query: &crate::query::UserListHooksQuery,
    ) -> ItemStream<forgejo_model::Hook> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/hooks").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List the authenticated user's webhooks
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/hooks` — needs the `read:user` token scope.
    pub async fn list_hooks_page(
        &self,
        query: &crate::query::UserListHooksQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Hook>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/hooks").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// List the given user's public keys
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /users/{username}/keys` — needs the `read:user` token scope.
    pub fn list_keys(
        &self,
        username: &str,
        query: &crate::query::UserListKeysQuery,
    ) -> ItemStream<forgejo_model::PublicKey> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/users/{}/keys", encode::seg(username))).scope("read:user"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the given user's public keys
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /users/{username}/keys` — needs the `read:user` token scope.
    pub async fn list_keys_page(
        &self,
        username: &str,
        query: &crate::query::UserListKeysQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::PublicKey>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/users/{}/keys", encode::seg(username))).scope("read:user"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List the authenticated user's oauth2 applications
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/applications/oauth2` — needs the `read:user` token scope.
    pub fn list_oauth2_applications(
        &self,
        query: &crate::query::UserGetOAuth2ApplicationsQuery,
    ) -> ItemStream<forgejo_model::OAuth2Application> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/applications/oauth2").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List the authenticated user's oauth2 applications
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/applications/oauth2` — needs the `read:user` token scope.
    pub async fn list_oauth2_applications_page(
        &self,
        query: &crate::query::UserGetOAuth2ApplicationsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::OAuth2Application>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/applications/oauth2").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// List the artifacts affecting the authenticated user's quota
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/quota/artifacts` — needs the `read:user` token scope.
    pub fn list_quota_artifacts(
        &self,
        query: &crate::query::UserListQuotaArtifactsQuery,
    ) -> ItemStream<forgejo_model::QuotaUsedArtifact> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/quota/artifacts").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List the artifacts affecting the authenticated user's quota
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/quota/artifacts` — needs the `read:user` token scope.
    pub async fn list_quota_artifacts_page(
        &self,
        query: &crate::query::UserListQuotaArtifactsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::QuotaUsedArtifact>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/quota/artifacts").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// List the attachments affecting the authenticated user's quota
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/quota/attachments` — needs the `read:user` token scope.
    pub fn list_quota_attachments(
        &self,
        query: &crate::query::UserListQuotaAttachmentsQuery,
    ) -> ItemStream<forgejo_model::QuotaUsedAttachment> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/quota/attachments").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List the attachments affecting the authenticated user's quota
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/quota/attachments` — needs the `read:user` token scope.
    pub async fn list_quota_attachments_page(
        &self,
        query: &crate::query::UserListQuotaAttachmentsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::QuotaUsedAttachment>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/quota/attachments").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// List the packages affecting the authenticated user's quota
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/quota/packages` — needs the `read:user` token scope.
    pub fn list_quota_packages(
        &self,
        query: &crate::query::UserListQuotaPackagesQuery,
    ) -> ItemStream<forgejo_model::QuotaUsedPackage> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/quota/packages").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List the packages affecting the authenticated user's quota
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/quota/packages` — needs the `read:user` token scope.
    pub async fn list_quota_packages_page(
        &self,
        query: &crate::query::UserListQuotaPackagesQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::QuotaUsedPackage>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/quota/packages").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// List the repos owned by the given user
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /users/{username}/repos` — needs the `read:user` token scope.
    pub fn list_repos(
        &self,
        username: &str,
        query: &crate::query::UserListReposQuery,
    ) -> ItemStream<forgejo_model::Repository> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/users/{}/repos", encode::seg(username))).scope("read:user"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the repos owned by the given user
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /users/{username}/repos` — needs the `read:user` token scope.
    pub async fn list_repos_page(
        &self,
        username: &str,
        query: &crate::query::UserListReposQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Repository>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/users/{}/repos", encode::seg(username))).scope("read:user"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// The repos that the given user has starred
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /users/{username}/starred` — needs the `read:user` token scope.
    pub fn list_starred(
        &self,
        username: &str,
        query: &crate::query::UserListStarredQuery,
    ) -> ItemStream<forgejo_model::Repository> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/users/{}/starred", encode::seg(username)))
                    .scope("read:user"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// The repos that the given user has starred
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /users/{username}/starred` — needs the `read:user` token scope.
    pub async fn list_starred_page(
        &self,
        username: &str,
        query: &crate::query::UserListStarredQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Repository>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/users/{}/starred", encode::seg(username)))
                    .scope("read:user"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List the repositories watched by a user
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /users/{username}/subscriptions` — needs the `read:user` token scope.
    pub fn list_subscriptions(
        &self,
        username: &str,
        query: &crate::query::UserListSubscriptionsQuery,
    ) -> ItemStream<forgejo_model::Repository> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/users/{}/subscriptions", encode::seg(username)))
                    .scope("read:user"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the repositories watched by a user
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /users/{username}/subscriptions` — needs the `read:user` token scope.
    pub async fn list_subscriptions_page(
        &self,
        username: &str,
        query: &crate::query::UserListSubscriptionsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Repository>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/users/{}/subscriptions", encode::seg(username)))
                    .scope("read:user"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List all the teams a user belongs to
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Forgejo silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/teams` — needs the `read:user` token scope.
    pub fn list_teams(
        &self,
        query: &crate::query::UserListTeamsQuery,
    ) -> ItemStream<forgejo_model::Team> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/teams").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// List all the teams a user belongs to
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/teams` — needs the `read:user` token scope.
    pub async fn list_teams_page(
        &self,
        query: &crate::query::UserListTeamsQuery,
        paging: Paging,
    ) -> Result<(Vec<forgejo_model::Team>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/teams").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
}
