// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! Query-parameter structs for `run`.
use forgejo_core::http::Request;
/// Query parameters for `ListActionRunArtifacts`.
///
/// `GET /repos/{owner}/{repo}/actions/runs/{run_id}/artifacts`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ListActionRunArtifactsQuery {
    /// page size of results, default maximum page size is 50
    pub limit: Option<i32>,
    /// filter by artifact name
    pub name: Option<String>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
}
impl ListActionRunArtifactsQuery {
    /// page size of results, default maximum page size is 50
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// filter by artifact name
    pub fn with_name(mut self, name: &str) -> Self {
        self.name = Some(name.to_owned());
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.name {
            out.push(("name", v.clone()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `ListActionRuns`.
///
/// `GET /repos/{owner}/{repo}/actions/runs`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ListActionRunsQuery {
    /// Returns workflow run triggered by the specified events. For example, `push`, `pull_request` or
    /// `workflow_dispatch`.
    pub event: Vec<String>,
    /// Only returns workflow runs that are associated with the specified head_sha.
    pub head_sha: Option<String>,
    /// page size of results, default maximum page size is 50
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// Only return workflow runs that involve the given Git reference, for example, `refs/heads/main`.
    pub r#ref: Option<String>,
    /// Returns the workflow run associated with the run number.
    pub run_number: Option<i64>,
    /// Returns workflow runs with the check run status or conclusion that is specified. For example, a
    /// conclusion can be success or a status can be in_progress. Only Forgejo Actions can set a status
    /// of waiting, pending, or requested.
    ///
    /// Known values: `unknown`, `waiting`, `running`, `success`, `failure`, `cancelled`, `skipped`, `blocked`. Not enforced — a newer server may accept others.
    pub status: Vec<String>,
    /// Only return workflow runs that involve the given workflow ID.
    pub workflow_id: Option<String>,
}
impl ListActionRunsQuery {
    /// Returns workflow run triggered by the specified events. For example, `push`, `pull_request` or
    /// `workflow_dispatch`.
    pub fn with_event(mut self, event: Vec<String>) -> Self {
        self.event = event;
        self
    }
    /// Only returns workflow runs that are associated with the specified head_sha.
    pub fn with_head_sha(mut self, head_sha: &str) -> Self {
        self.head_sha = Some(head_sha.to_owned());
        self
    }
    /// page size of results, default maximum page size is 50
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// Only return workflow runs that involve the given Git reference, for example, `refs/heads/main`.
    pub fn with_ref(mut self, r#ref: &str) -> Self {
        self.r#ref = Some(r#ref.to_owned());
        self
    }
    /// Returns the workflow run associated with the run number.
    pub fn with_run_number(mut self, run_number: i64) -> Self {
        self.run_number = Some(run_number);
        self
    }
    /// Returns workflow runs with the check run status or conclusion that is specified. For example, a
    /// conclusion can be success or a status can be in_progress. Only Forgejo Actions can set a status
    /// of waiting, pending, or requested.
    ///
    /// Known values: `unknown`, `waiting`, `running`, `success`, `failure`, `cancelled`, `skipped`, `blocked`. Not enforced — a newer server may accept others.
    pub fn with_status(mut self, status: Vec<String>) -> Self {
        self.status = status;
        self
    }
    /// Only return workflow runs that involve the given workflow ID.
    pub fn with_workflow_id(mut self, workflow_id: &str) -> Self {
        self.workflow_id = Some(workflow_id.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        for v in &self.event {
            out.push(("event", v.clone()));
        }
        if let Some(v) = &self.head_sha {
            out.push(("head_sha", v.clone()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.r#ref {
            out.push(("ref", v.clone()));
        }
        if let Some(v) = &self.run_number {
            out.push(("run_number", v.to_string()));
        }
        for v in &self.status {
            out.push(("status", v.clone()));
        }
        if let Some(v) = &self.workflow_id {
            out.push(("workflow_id", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
