// @generated by `cargo xtask codegen` from spec/forgejo-16.0.5.json
// (sha256:6d3563a56631a92b). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! Query-parameter structs for `issue`.
use forgejo_core::http::Request;
/// Query parameters for `issueCreateIssueAttachment`.
///
/// `POST /repos/{owner}/{repo}/issues/{index}/assets`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueCreateIssueAttachmentQuery {
    /// name of the attachment
    pub name: Option<String>,
    /// time of the attachment's creation. This is a timestamp in RFC 3339 format
    pub updated_at: Option<forgejo_core::types::Timestamp>,
}
impl IssueCreateIssueAttachmentQuery {
    /// name of the attachment
    pub fn with_name(mut self, name: &str) -> Self {
        self.name = Some(name.to_owned());
        self
    }
    /// time of the attachment's creation. This is a timestamp in RFC 3339 format
    pub fn with_updated_at(mut self, updated_at: forgejo_core::types::Timestamp) -> Self {
        self.updated_at = Some(updated_at);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.name {
            out.push(("name", v.clone()));
        }
        if let Some(v) = &self.updated_at {
            out.push(("updated_at", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueCreateIssueCommentAttachment`.
///
/// `POST /repos/{owner}/{repo}/issues/comments/{id}/assets`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueCreateIssueCommentAttachmentQuery {
    /// name of the attachment
    pub name: Option<String>,
    /// time of the attachment's creation. This is a timestamp in RFC 3339 format
    pub updated_at: Option<forgejo_core::types::Timestamp>,
}
impl IssueCreateIssueCommentAttachmentQuery {
    /// name of the attachment
    pub fn with_name(mut self, name: &str) -> Self {
        self.name = Some(name.to_owned());
        self
    }
    /// time of the attachment's creation. This is a timestamp in RFC 3339 format
    pub fn with_updated_at(mut self, updated_at: forgejo_core::types::Timestamp) -> Self {
        self.updated_at = Some(updated_at);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.name {
            out.push(("name", v.clone()));
        }
        if let Some(v) = &self.updated_at {
            out.push(("updated_at", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueGetComments`.
///
/// `GET /repos/{owner}/{repo}/issues/{index}/comments`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueGetCommentsQuery {
    /// if provided, only comments updated before the provided time are returned.
    pub before: Option<forgejo_core::types::Timestamp>,
    /// if provided, only comments updated since the specified time are returned.
    pub since: Option<forgejo_core::types::Timestamp>,
}
impl IssueGetCommentsQuery {
    /// if provided, only comments updated before the provided time are returned.
    pub fn with_before(mut self, before: forgejo_core::types::Timestamp) -> Self {
        self.before = Some(before);
        self
    }
    /// if provided, only comments updated since the specified time are returned.
    pub fn with_since(mut self, since: forgejo_core::types::Timestamp) -> Self {
        self.since = Some(since);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.before {
            out.push(("before", v.to_string()));
        }
        if let Some(v) = &self.since {
            out.push(("since", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueGetCommentsAndTimeline`.
///
/// `GET /repos/{owner}/{repo}/issues/{index}/timeline`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueGetCommentsAndTimelineQuery {
    /// if provided, only comments updated before the provided time are returned.
    pub before: Option<forgejo_core::types::Timestamp>,
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// if provided, only comments updated since the specified time are returned.
    pub since: Option<forgejo_core::types::Timestamp>,
}
impl IssueGetCommentsAndTimelineQuery {
    /// if provided, only comments updated before the provided time are returned.
    pub fn with_before(mut self, before: forgejo_core::types::Timestamp) -> Self {
        self.before = Some(before);
        self
    }
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// if provided, only comments updated since the specified time are returned.
    pub fn with_since(mut self, since: forgejo_core::types::Timestamp) -> Self {
        self.since = Some(since);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.before {
            out.push(("before", v.to_string()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.since {
            out.push(("since", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueGetIssueReactions`.
///
/// `GET /repos/{owner}/{repo}/issues/{index}/reactions`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueGetIssueReactionsQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
}
impl IssueGetIssueReactionsQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueGetMilestonesList`.
///
/// `GET /repos/{owner}/{repo}/milestones`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueGetMilestonesListQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// filter by milestone name
    pub name: Option<String>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// Milestone state, Recognized values are open, closed and all. Defaults to "open"
    pub state: Option<String>,
}
impl IssueGetMilestonesListQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// filter by milestone name
    pub fn with_name(mut self, name: &str) -> Self {
        self.name = Some(name.to_owned());
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// Milestone state, Recognized values are open, closed and all. Defaults to "open"
    pub fn with_state(mut self, state: &str) -> Self {
        self.state = Some(state.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.name {
            out.push(("name", v.clone()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.state {
            out.push(("state", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueGetRepoComments`.
///
/// `GET /repos/{owner}/{repo}/issues/comments`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueGetRepoCommentsQuery {
    /// if provided, only comments updated before the provided time are returned.
    pub before: Option<forgejo_core::types::Timestamp>,
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// if provided, only comments updated since the provided time are returned.
    pub since: Option<forgejo_core::types::Timestamp>,
}
impl IssueGetRepoCommentsQuery {
    /// if provided, only comments updated before the provided time are returned.
    pub fn with_before(mut self, before: forgejo_core::types::Timestamp) -> Self {
        self.before = Some(before);
        self
    }
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// if provided, only comments updated since the provided time are returned.
    pub fn with_since(mut self, since: forgejo_core::types::Timestamp) -> Self {
        self.since = Some(since);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.before {
            out.push(("before", v.to_string()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.since {
            out.push(("since", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueListBlocks`.
///
/// `GET /repos/{owner}/{repo}/issues/{index}/blocks`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueListBlocksQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
}
impl IssueListBlocksQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueListIssueDependencies`.
///
/// `GET /repos/{owner}/{repo}/issues/{index}/dependencies`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueListIssueDependenciesQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
}
impl IssueListIssueDependenciesQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueListIssues`.
///
/// `GET /repos/{owner}/{repo}/issues`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueListIssuesQuery {
    /// Only show items for which the given user is assigned
    pub assigned_by: Option<String>,
    /// Only show items updated before the given time. This is a timestamp in RFC 3339 format
    pub before: Option<forgejo_core::types::Timestamp>,
    /// Only show items which were created by the given user
    pub created_by: Option<String>,
    /// comma separated list of labels. Fetch only issues that have any of this labels. Non existent
    /// labels are discarded
    pub labels: Option<String>,
    /// page size of results
    pub limit: Option<i32>,
    /// Only show items in which the given user was mentioned
    pub mentioned_by: Option<String>,
    /// comma separated list of milestone names or ids. It uses names and fall back to ids. Fetch only
    /// issues that have any of this milestones. Non existent milestones are discarded
    pub milestones: Option<String>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// search string
    pub q: Option<String>,
    /// Only show items updated after the given time. This is a timestamp in RFC 3339 format
    pub since: Option<forgejo_core::types::Timestamp>,
    /// Type of sort
    ///
    /// The server defaults this to `latest` when it is absent.
    /// Known values: `relevance`, `latest`, `oldest`, `recentupdate`, `leastupdate`, `mostcomment`, `leastcomment`, `nearduedate`, `farduedate`. Not enforced — a newer server may accept others.
    pub sort: Option<String>,
    /// whether issue is open or closed
    ///
    /// Known values: `closed`, `open`, `all`. Not enforced — a newer server may accept others.
    pub state: Option<String>,
    /// filter by type (issues / pulls) if set
    ///
    /// Known values: `issues`, `pulls`. Not enforced — a newer server may accept others.
    pub r#type: Option<String>,
}
impl IssueListIssuesQuery {
    /// Only show items for which the given user is assigned
    pub fn with_assigned_by(mut self, assigned_by: &str) -> Self {
        self.assigned_by = Some(assigned_by.to_owned());
        self
    }
    /// Only show items updated before the given time. This is a timestamp in RFC 3339 format
    pub fn with_before(mut self, before: forgejo_core::types::Timestamp) -> Self {
        self.before = Some(before);
        self
    }
    /// Only show items which were created by the given user
    pub fn with_created_by(mut self, created_by: &str) -> Self {
        self.created_by = Some(created_by.to_owned());
        self
    }
    /// comma separated list of labels. Fetch only issues that have any of this labels. Non existent
    /// labels are discarded
    pub fn with_labels(mut self, labels: &str) -> Self {
        self.labels = Some(labels.to_owned());
        self
    }
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// Only show items in which the given user was mentioned
    pub fn with_mentioned_by(mut self, mentioned_by: &str) -> Self {
        self.mentioned_by = Some(mentioned_by.to_owned());
        self
    }
    /// comma separated list of milestone names or ids. It uses names and fall back to ids. Fetch only
    /// issues that have any of this milestones. Non existent milestones are discarded
    pub fn with_milestones(mut self, milestones: &str) -> Self {
        self.milestones = Some(milestones.to_owned());
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// search string
    pub fn with_q(mut self, q: &str) -> Self {
        self.q = Some(q.to_owned());
        self
    }
    /// Only show items updated after the given time. This is a timestamp in RFC 3339 format
    pub fn with_since(mut self, since: forgejo_core::types::Timestamp) -> Self {
        self.since = Some(since);
        self
    }
    /// Type of sort
    ///
    /// The server defaults this to `latest` when it is absent.
    /// Known values: `relevance`, `latest`, `oldest`, `recentupdate`, `leastupdate`, `mostcomment`, `leastcomment`, `nearduedate`, `farduedate`. Not enforced — a newer server may accept others.
    pub fn with_sort(mut self, sort: &str) -> Self {
        self.sort = Some(sort.to_owned());
        self
    }
    /// whether issue is open or closed
    ///
    /// Known values: `closed`, `open`, `all`. Not enforced — a newer server may accept others.
    pub fn with_state(mut self, state: &str) -> Self {
        self.state = Some(state.to_owned());
        self
    }
    /// filter by type (issues / pulls) if set
    ///
    /// Known values: `issues`, `pulls`. Not enforced — a newer server may accept others.
    pub fn with_type(mut self, r#type: &str) -> Self {
        self.r#type = Some(r#type.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.assigned_by {
            out.push(("assigned_by", v.clone()));
        }
        if let Some(v) = &self.before {
            out.push(("before", v.to_string()));
        }
        if let Some(v) = &self.created_by {
            out.push(("created_by", v.clone()));
        }
        if let Some(v) = &self.labels {
            out.push(("labels", v.clone()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.mentioned_by {
            out.push(("mentioned_by", v.clone()));
        }
        if let Some(v) = &self.milestones {
            out.push(("milestones", v.clone()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.q {
            out.push(("q", v.clone()));
        }
        if let Some(v) = &self.since {
            out.push(("since", v.to_string()));
        }
        if let Some(v) = &self.sort {
            out.push(("sort", v.clone()));
        }
        if let Some(v) = &self.state {
            out.push(("state", v.clone()));
        }
        if let Some(v) = &self.r#type {
            out.push(("type", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueListLabels`.
///
/// `GET /repos/{owner}/{repo}/labels`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueListLabelsQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// Specifies the sorting method: mostissues, leastissues, or reversealphabetically.
    ///
    /// Known values: `mostissues`, `leastissues`, `reversealphabetically`. Not enforced — a newer server may accept others.
    pub sort: Option<String>,
}
impl IssueListLabelsQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// Specifies the sorting method: mostissues, leastissues, or reversealphabetically.
    ///
    /// Known values: `mostissues`, `leastissues`, `reversealphabetically`. Not enforced — a newer server may accept others.
    pub fn with_sort(mut self, sort: &str) -> Self {
        self.sort = Some(sort.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.sort {
            out.push(("sort", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueSearchIssues`.
///
/// `GET /repos/issues/search`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueSearchIssuesQuery {
    /// Filter issues or pulls assigned to the authenticated user
    ///
    /// The server defaults this to `false` when it is absent.
    pub assigned: Option<bool>,
    /// Only show issues updated before the given time (RFC 3339 format)
    pub before: Option<forgejo_core::types::Timestamp>,
    /// Filter issues or pulls created by the authenticated user
    ///
    /// The server defaults this to `false` when it is absent.
    pub created: Option<bool>,
    /// Comma-separated list of label names. Fetch only issues that have any of these labels. Non
    /// existent labels are discarded.
    pub labels: Option<String>,
    /// Number of items per page
    pub limit: Option<i32>,
    /// Filter issues or pulls mentioning the authenticated user
    ///
    /// The server defaults this to `false` when it is absent.
    pub mentioned: Option<bool>,
    /// Comma-separated list of milestone names. Fetch only issues that have any of these milestones.
    /// Non existent milestones are discarded.
    pub milestones: Option<String>,
    /// Filter by repository owner
    pub owner: Option<String>,
    /// Page number of results to return (1-based)
    ///
    /// The server defaults this to `1` when it is absent.
    pub page: Option<i32>,
    /// Repository ID to prioritize in the results
    pub priority_repo_id: Option<i64>,
    /// Search string
    pub q: Option<String>,
    /// Filter pull requests where the authenticated user's review was requested
    ///
    /// The server defaults this to `false` when it is absent.
    pub review_requested: Option<bool>,
    /// Filter pull requests reviewed by the authenticated user
    ///
    /// The server defaults this to `false` when it is absent.
    pub reviewed: Option<bool>,
    /// Only show issues updated after the given time (RFC 3339 format)
    pub since: Option<forgejo_core::types::Timestamp>,
    /// Type of sort
    ///
    /// The server defaults this to `latest` when it is absent.
    /// Known values: `relevance`, `latest`, `oldest`, `recentupdate`, `leastupdate`, `mostcomment`, `leastcomment`, `nearduedate`, `farduedate`. Not enforced — a newer server may accept others.
    pub sort: Option<String>,
    /// State of the issue
    ///
    /// The server defaults this to `open` when it is absent.
    /// Known values: `open`, `closed`, `all`. Not enforced — a newer server may accept others.
    pub state: Option<String>,
    /// Filter by team (requires organization owner parameter)
    pub team: Option<String>,
    /// Filter by issue type
    ///
    /// Known values: `issues`, `pulls`. Not enforced — a newer server may accept others.
    pub r#type: Option<String>,
}
impl IssueSearchIssuesQuery {
    /// Filter issues or pulls assigned to the authenticated user
    ///
    /// The server defaults this to `false` when it is absent.
    pub fn with_assigned(mut self, assigned: bool) -> Self {
        self.assigned = Some(assigned);
        self
    }
    /// Only show issues updated before the given time (RFC 3339 format)
    pub fn with_before(mut self, before: forgejo_core::types::Timestamp) -> Self {
        self.before = Some(before);
        self
    }
    /// Filter issues or pulls created by the authenticated user
    ///
    /// The server defaults this to `false` when it is absent.
    pub fn with_created(mut self, created: bool) -> Self {
        self.created = Some(created);
        self
    }
    /// Comma-separated list of label names. Fetch only issues that have any of these labels. Non
    /// existent labels are discarded.
    pub fn with_labels(mut self, labels: &str) -> Self {
        self.labels = Some(labels.to_owned());
        self
    }
    /// Number of items per page
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// Filter issues or pulls mentioning the authenticated user
    ///
    /// The server defaults this to `false` when it is absent.
    pub fn with_mentioned(mut self, mentioned: bool) -> Self {
        self.mentioned = Some(mentioned);
        self
    }
    /// Comma-separated list of milestone names. Fetch only issues that have any of these milestones.
    /// Non existent milestones are discarded.
    pub fn with_milestones(mut self, milestones: &str) -> Self {
        self.milestones = Some(milestones.to_owned());
        self
    }
    /// Filter by repository owner
    pub fn with_owner(mut self, owner: &str) -> Self {
        self.owner = Some(owner.to_owned());
        self
    }
    /// Page number of results to return (1-based)
    ///
    /// The server defaults this to `1` when it is absent.
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// Repository ID to prioritize in the results
    pub fn with_priority_repo_id(mut self, priority_repo_id: i64) -> Self {
        self.priority_repo_id = Some(priority_repo_id);
        self
    }
    /// Search string
    pub fn with_q(mut self, q: &str) -> Self {
        self.q = Some(q.to_owned());
        self
    }
    /// Filter pull requests where the authenticated user's review was requested
    ///
    /// The server defaults this to `false` when it is absent.
    pub fn with_review_requested(mut self, review_requested: bool) -> Self {
        self.review_requested = Some(review_requested);
        self
    }
    /// Filter pull requests reviewed by the authenticated user
    ///
    /// The server defaults this to `false` when it is absent.
    pub fn with_reviewed(mut self, reviewed: bool) -> Self {
        self.reviewed = Some(reviewed);
        self
    }
    /// Only show issues updated after the given time (RFC 3339 format)
    pub fn with_since(mut self, since: forgejo_core::types::Timestamp) -> Self {
        self.since = Some(since);
        self
    }
    /// Type of sort
    ///
    /// The server defaults this to `latest` when it is absent.
    /// Known values: `relevance`, `latest`, `oldest`, `recentupdate`, `leastupdate`, `mostcomment`, `leastcomment`, `nearduedate`, `farduedate`. Not enforced — a newer server may accept others.
    pub fn with_sort(mut self, sort: &str) -> Self {
        self.sort = Some(sort.to_owned());
        self
    }
    /// State of the issue
    ///
    /// The server defaults this to `open` when it is absent.
    /// Known values: `open`, `closed`, `all`. Not enforced — a newer server may accept others.
    pub fn with_state(mut self, state: &str) -> Self {
        self.state = Some(state.to_owned());
        self
    }
    /// Filter by team (requires organization owner parameter)
    pub fn with_team(mut self, team: &str) -> Self {
        self.team = Some(team.to_owned());
        self
    }
    /// Filter by issue type
    ///
    /// Known values: `issues`, `pulls`. Not enforced — a newer server may accept others.
    pub fn with_type(mut self, r#type: &str) -> Self {
        self.r#type = Some(r#type.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.assigned {
            out.push(("assigned", v.to_string()));
        }
        if let Some(v) = &self.before {
            out.push(("before", v.to_string()));
        }
        if let Some(v) = &self.created {
            out.push(("created", v.to_string()));
        }
        if let Some(v) = &self.labels {
            out.push(("labels", v.clone()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.mentioned {
            out.push(("mentioned", v.to_string()));
        }
        if let Some(v) = &self.milestones {
            out.push(("milestones", v.clone()));
        }
        if let Some(v) = &self.owner {
            out.push(("owner", v.clone()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.priority_repo_id {
            out.push(("priority_repo_id", v.to_string()));
        }
        if let Some(v) = &self.q {
            out.push(("q", v.clone()));
        }
        if let Some(v) = &self.review_requested {
            out.push(("review_requested", v.to_string()));
        }
        if let Some(v) = &self.reviewed {
            out.push(("reviewed", v.to_string()));
        }
        if let Some(v) = &self.since {
            out.push(("since", v.to_string()));
        }
        if let Some(v) = &self.sort {
            out.push(("sort", v.clone()));
        }
        if let Some(v) = &self.state {
            out.push(("state", v.clone()));
        }
        if let Some(v) = &self.team {
            out.push(("team", v.clone()));
        }
        if let Some(v) = &self.r#type {
            out.push(("type", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueSubscriptions`.
///
/// `GET /repos/{owner}/{repo}/issues/{index}/subscriptions`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueSubscriptionsQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
}
impl IssueSubscriptionsQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `issueTrackedTimes`.
///
/// `GET /repos/{owner}/{repo}/issues/{index}/times`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct IssueTrackedTimesQuery {
    /// Only show times updated before the given time. This is a timestamp in RFC 3339 format
    pub before: Option<forgejo_core::types::Timestamp>,
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// Only show times updated after the given time. This is a timestamp in RFC 3339 format
    pub since: Option<forgejo_core::types::Timestamp>,
    /// optional filter by user (available for issue managers)
    pub user: Option<String>,
}
impl IssueTrackedTimesQuery {
    /// Only show times updated before the given time. This is a timestamp in RFC 3339 format
    pub fn with_before(mut self, before: forgejo_core::types::Timestamp) -> Self {
        self.before = Some(before);
        self
    }
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// Only show times updated after the given time. This is a timestamp in RFC 3339 format
    pub fn with_since(mut self, since: forgejo_core::types::Timestamp) -> Self {
        self.since = Some(since);
        self
    }
    /// optional filter by user (available for issue managers)
    pub fn with_user(mut self, user: &str) -> Self {
        self.user = Some(user.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.before {
            out.push(("before", v.to_string()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.since {
            out.push(("since", v.to_string()));
        }
        if let Some(v) = &self.user {
            out.push(("user", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
