// @generated by `cargo xtask codegen` from spec/forgejo-16.0.4.json
// (sha256:146241901a8a5194). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! Query-parameter structs for `repo_g`.
use forgejo_core::http::Request;
/// Query parameters for `repoGetActionJobLogs`.
///
/// `GET /repos/{owner}/{repo}/actions/jobs/{job_id}/logs`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetActionJobLogsQuery {
    /// 1-based attempt number matching the value of `attempt` in the job listing; omit to fetch the
    /// latest attempt of the job
    pub attempt: Option<i64>,
}
impl RepoGetActionJobLogsQuery {
    /// 1-based attempt number matching the value of `attempt` in the job listing; omit to fetch the
    /// latest attempt of the job
    pub fn with_attempt(mut self, attempt: i64) -> Self {
        self.attempt = Some(attempt);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.attempt {
            out.push(("attempt", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetAllCommits`.
///
/// `GET /repos/{owner}/{repo}/commits`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetAllCommitsQuery {
    /// include a list of affected files for every commit (disable for speedup, default 'true')
    pub files: Option<bool>,
    /// page size of results
    pub limit: Option<i32>,
    /// commits that match the given specifier will not be listed.
    pub not: Option<String>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// filepath of a file/dir
    pub path: Option<String>,
    /// SHA or branch to start listing commits from (usually 'master')
    pub sha: Option<String>,
    /// include diff stats for every commit (disable for speedup, default 'true')
    pub stat: Option<bool>,
    /// include verification for every commit (disable for speedup, default 'true')
    pub verification: Option<bool>,
}
impl RepoGetAllCommitsQuery {
    /// include a list of affected files for every commit (disable for speedup, default 'true')
    pub fn with_files(mut self, files: bool) -> Self {
        self.files = Some(files);
        self
    }
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// commits that match the given specifier will not be listed.
    pub fn with_not(mut self, not: &str) -> Self {
        self.not = Some(not.to_owned());
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// filepath of a file/dir
    pub fn with_path(mut self, path: &str) -> Self {
        self.path = Some(path.to_owned());
        self
    }
    /// SHA or branch to start listing commits from (usually 'master')
    pub fn with_sha(mut self, sha: &str) -> Self {
        self.sha = Some(sha.to_owned());
        self
    }
    /// include diff stats for every commit (disable for speedup, default 'true')
    pub fn with_stat(mut self, stat: bool) -> Self {
        self.stat = Some(stat);
        self
    }
    /// include verification for every commit (disable for speedup, default 'true')
    pub fn with_verification(mut self, verification: bool) -> Self {
        self.verification = Some(verification);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.files {
            out.push(("files", v.to_string()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.not {
            out.push(("not", v.clone()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.path {
            out.push(("path", v.clone()));
        }
        if let Some(v) = &self.sha {
            out.push(("sha", v.clone()));
        }
        if let Some(v) = &self.stat {
            out.push(("stat", v.to_string()));
        }
        if let Some(v) = &self.verification {
            out.push(("verification", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetCombinedStatusByRef`.
///
/// `GET /repos/{owner}/{repo}/commits/{ref}/status`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetCombinedStatusByRefQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
}
impl RepoGetCombinedStatusByRefQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetContents`.
///
/// `GET /repos/{owner}/{repo}/contents/{filepath}`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetContentsQuery {
    /// The name of the commit/branch/tag. Default the repository’s default branch (usually master)
    pub r#ref: Option<String>,
}
impl RepoGetContentsQuery {
    /// The name of the commit/branch/tag. Default the repository’s default branch (usually master)
    pub fn with_ref(mut self, r#ref: &str) -> Self {
        self.r#ref = Some(r#ref.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.r#ref {
            out.push(("ref", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetContentsList`.
///
/// `GET /repos/{owner}/{repo}/contents`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetContentsListQuery {
    /// The name of the commit/branch/tag. Default the repository’s default branch (usually master)
    pub r#ref: Option<String>,
}
impl RepoGetContentsListQuery {
    /// The name of the commit/branch/tag. Default the repository’s default branch (usually master)
    pub fn with_ref(mut self, r#ref: &str) -> Self {
        self.r#ref = Some(r#ref.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.r#ref {
            out.push(("ref", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetEditorConfig`.
///
/// `GET /repos/{owner}/{repo}/editorconfig/{filepath}`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetEditorConfigQuery {
    /// The name of the commit/branch/tag. Default the repository’s default branch (usually master)
    pub r#ref: Option<String>,
}
impl RepoGetEditorConfigQuery {
    /// The name of the commit/branch/tag. Default the repository’s default branch (usually master)
    pub fn with_ref(mut self, r#ref: &str) -> Self {
        self.r#ref = Some(r#ref.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.r#ref {
            out.push(("ref", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetNote`.
///
/// `GET /repos/{owner}/{repo}/git/notes/{sha}`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetNoteQuery {
    /// include a list of affected files for every commit (disable for speedup, default 'true')
    pub files: Option<bool>,
    /// include verification for every commit (disable for speedup, default 'true')
    pub verification: Option<bool>,
}
impl RepoGetNoteQuery {
    /// include a list of affected files for every commit (disable for speedup, default 'true')
    pub fn with_files(mut self, files: bool) -> Self {
        self.files = Some(files);
        self
    }
    /// include verification for every commit (disable for speedup, default 'true')
    pub fn with_verification(mut self, verification: bool) -> Self {
        self.verification = Some(verification);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.files {
            out.push(("files", v.to_string()));
        }
        if let Some(v) = &self.verification {
            out.push(("verification", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetPullRequestCommits`.
///
/// `GET /repos/{owner}/{repo}/pulls/{index}/commits`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetPullRequestCommitsQuery {
    /// include a list of affected files for every commit (disable for speedup, default 'true')
    pub files: Option<bool>,
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// include verification for every commit (disable for speedup, default 'true')
    pub verification: Option<bool>,
}
impl RepoGetPullRequestCommitsQuery {
    /// include a list of affected files for every commit (disable for speedup, default 'true')
    pub fn with_files(mut self, files: bool) -> Self {
        self.files = Some(files);
        self
    }
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// include verification for every commit (disable for speedup, default 'true')
    pub fn with_verification(mut self, verification: bool) -> Self {
        self.verification = Some(verification);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.files {
            out.push(("files", v.to_string()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.verification {
            out.push(("verification", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetPullRequestFiles`.
///
/// `GET /repos/{owner}/{repo}/pulls/{index}/files`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetPullRequestFilesQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// skip to given file
    pub skip_to: Option<String>,
    /// whitespace behavior
    ///
    /// Known values: `ignore-all`, `ignore-change`, `ignore-eol`, `show-all`. Not enforced — a newer server may accept others.
    pub whitespace: Option<String>,
}
impl RepoGetPullRequestFilesQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// skip to given file
    pub fn with_skip_to(mut self, skip_to: &str) -> Self {
        self.skip_to = Some(skip_to.to_owned());
        self
    }
    /// whitespace behavior
    ///
    /// Known values: `ignore-all`, `ignore-change`, `ignore-eol`, `show-all`. Not enforced — a newer server may accept others.
    pub fn with_whitespace(mut self, whitespace: &str) -> Self {
        self.whitespace = Some(whitespace.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.skip_to {
            out.push(("skip-to", v.clone()));
        }
        if let Some(v) = &self.whitespace {
            out.push(("whitespace", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetRawFile`.
///
/// `GET /repos/{owner}/{repo}/raw/{filepath}`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetRawFileQuery {
    /// The name of the commit/branch/tag. Default the repository’s default branch (usually master)
    pub r#ref: Option<String>,
}
impl RepoGetRawFileQuery {
    /// The name of the commit/branch/tag. Default the repository’s default branch (usually master)
    pub fn with_ref(mut self, r#ref: &str) -> Self {
        self.r#ref = Some(r#ref.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.r#ref {
            out.push(("ref", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetRawFileOrLFS`.
///
/// `GET /repos/{owner}/{repo}/media/{filepath}`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetRawFileOrLfsQuery {
    /// The name of the commit/branch/tag. Default the repository’s default branch (usually master)
    pub r#ref: Option<String>,
}
impl RepoGetRawFileOrLfsQuery {
    /// The name of the commit/branch/tag. Default the repository’s default branch (usually master)
    pub fn with_ref(mut self, r#ref: &str) -> Self {
        self.r#ref = Some(r#ref.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.r#ref {
            out.push(("ref", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `getRepoRunners`.
///
/// `GET /repos/{owner}/{repo}/actions/runners`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetRepoRunnersQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// whether to include all visible runners (true) or only those that are directly owned by the
    /// repository (false)
    pub visible: Option<bool>,
}
impl GetRepoRunnersQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// whether to include all visible runners (true) or only those that are directly owned by the
    /// repository (false)
    pub fn with_visible(mut self, visible: bool) -> Self {
        self.visible = Some(visible);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.visible {
            out.push(("visible", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `getRepoVariablesList`.
///
/// `GET /repos/{owner}/{repo}/actions/variables`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetRepoVariablesListQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
}
impl GetRepoVariablesListQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetSingleCommit`.
///
/// `GET /repos/{owner}/{repo}/git/commits/{sha}`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetSingleCommitQuery {
    /// include a list of affected files for every commit (disable for speedup, default 'true')
    pub files: Option<bool>,
    /// include diff stats for every commit (disable for speedup, default 'true')
    pub stat: Option<bool>,
    /// include verification for every commit (disable for speedup, default 'true')
    pub verification: Option<bool>,
}
impl RepoGetSingleCommitQuery {
    /// include a list of affected files for every commit (disable for speedup, default 'true')
    pub fn with_files(mut self, files: bool) -> Self {
        self.files = Some(files);
        self
    }
    /// include diff stats for every commit (disable for speedup, default 'true')
    pub fn with_stat(mut self, stat: bool) -> Self {
        self.stat = Some(stat);
        self
    }
    /// include verification for every commit (disable for speedup, default 'true')
    pub fn with_verification(mut self, verification: bool) -> Self {
        self.verification = Some(verification);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.files {
            out.push(("files", v.to_string()));
        }
        if let Some(v) = &self.stat {
            out.push(("stat", v.to_string()));
        }
        if let Some(v) = &self.verification {
            out.push(("verification", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetWikiPageRevisions`.
///
/// `GET /repos/{owner}/{repo}/wiki/revisions/{pageName}`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetWikiPageRevisionsQuery {
    /// page number of results to return (1-based)
    pub page: Option<i32>,
}
impl RepoGetWikiPageRevisionsQuery {
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `repoGetWikiPages`.
///
/// `GET /repos/{owner}/{repo}/wiki/pages`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoGetWikiPagesQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
}
impl RepoGetWikiPagesQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
